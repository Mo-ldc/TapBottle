# 2026-09-24

## 第二十一轮：落地判定改纯概率制（用户拍板）
- **口径**：所有瓶子一个口径 —— 成功树立 **50%**（倒立 10% / 正立 40%），失败 50%；各阶「翻转精通」`mastery` 每级 **+5%**，满 **10 级 = 100%**（必成立）。
- **代码**：
  - `GameConfig.FLIP` 换成 `successBase 0.50 / successStep 0.05 / critShareOfSuccess 0.20 / masteryMax 10`；旧的 toleranceBase/Step、angleVar*、stabilityStep 全删。
  - `mastery` 词条七阶 `step` 0.10 → **0.05**（`mode: 'add'`）；⚠️ `base` 同时是价格基数（State.statCost），不能改成 0.5，效果值一律走 `G.successChance(tier)`。
  - `State.ts`：删 `rollLanding / outcomeOf / tierTolerance`，新增 `successChance(tier, lv?)` / `critChance` / `okShare` / `masteryMaxed` / `masteryMaxLevel`，`rollOutcome(tier)` 改为「先掷成败、再按 1:4 分倒立/正立」。
  - `State.stability`（p_stability）改为读节点自身 step —— **不再参与落地判定**（纯概率制后抗扰度无处可用）。
  - UI：`UpgradeRows.toleranceText` → `successText(tier)`（商店行尾显示「成功 50%」）；`statSub/statDesc` 对 mastery 特判，显示**总成功率** 50% → 55%（不是 +5% 增量）；`BottomPanel` 商店行改用 successText。
  - Locale：`bs_mastery` 「精通」→「翻转精通」，`bs_mastery_d` 用新的 `{tot}/{nexttot}` 占位符。
  - `GDD_Bottle_Flip_Inc_Cocos_v1.1.md` §3.1-3 加「实现口径修订（第十七轮）」引用块，角度容差模型降级为历史参考。
- **验证**：typecheck 0 错；构建 Finished 31s（exit 36 假失败）；产物 grep `successBase: 0.50` / `critShareOfSuccess` / `successChance` 确认。

## 第二十一轮补充：锁定「倒立 + 正立 = 成功率」（用户口径）
- 用户读到上一条回复里的示意表（正立 90% / 倒立 20% → 和 110%）后拍板：**倒立和正立加起来应该是 1** ——
  即成功池内部按基础 10%:40% = **1:4 归一化**，占比之和恒 = 1，**不是**「倒立永远固定 10%、正立吃掉全部增量」。
- 实现本来就是 1:4（`critShareOfSuccess 0.20`），本轮只是把不变量写死、去掉死代码：
  - `State.ts`：无参死 getter `okShare` → **`okChance(tier, lv?)` = successChance − critChance**（按定义互补）；
    注释写明 `critChance + okChance ≡ successChance`、精通只抬水位不改池内比例。
  - `GameConfig.FLIP.critShareOfSuccess` 注释改为「成功池内部 20% / 80%，占比和恒 = 1」。
  - `GDD...v1.1.md` §3.1-3 修订块补一条：精通 0 级 = 倒立 10% + 正立 40%，满级 = 倒立 20% + 正立 80% = 100%。
- 验证：typecheck 0 错；构建 `build Task (web-mobile) Finished in (10 s)`（exit 36 仍是 3000 端口被编辑器占用的假失败）；产物 grep `okChance` / `成功池内部` 确认。

## 第二十二轮：货币图标 + 广告角标 + 「点了没反应」修复（用户三条）
- **口径**：① 选项不隐藏消耗的货币 —— 价格按钮 = `[货币图标] 纯数字`；② 货币前加对应图标（金币 `ui/coin` / 瓶盖 `bottle/capchip_6`，与顶栏筹码同款）；③ 广告标识放选项右上角；④ 列表选项统一点击音效 `click`。
- **代码**：
  - `UpgradeRows.ts`：`RowSpec` 新增 `cur?: 'coin' | 'cap'`；有 cur 时 `label` 只放纯数字。makeCell 加 `curIc`（26 高，`setFrameH` 按贴图比例 contain），applyRow 里按 cur 切贴图 + 数字盒在「图右(CUR_TEXT_X=10,w48) / 铺满(PLAIN)」两布局间切，**contentSize 必须乘 TEXT_SS**（label 节点尺寸是栅格化尺寸）。
  - ad 态**不再 `btnLb.string=''`**（这就是"隐藏货币"的元凶）；ksp 摄像机角标缩到 **30** 贴格子右上角（AD_X/AD_Y 公式），有广告时数字盒整体左移 12px，「新」角标左移 46px 让位。
  - `BottomPanel.ts`：9 处价格行 `'$'+fmt / 'C '+fmt` → `fmt + cur`（statRow 是 `caps?'cap':'coin'`；满仓/已拥有行 cur=undefined）。已拥有/满级的 early return 全部补 Toast（machineRow/cursorRow/unlockTierRow/skillNodeRow/statRow，'#FFD98A'）。
  - makeCell 的 TOUCH_END 里加 `Res.I.play('click', 0.7)`。
- **★ 重大坑（同层后建节点吞触摸）**：newTag/adIcon 原来挂在 **cell** 上、建在 **btn** 之后 → 渲染在按钮上层，Cocos 命中按渲染序倒序取第一个命中节点 → 点到按钮右上角（角标覆盖区）时事件被角标吃掉、冒泡到 cell 就停，**按钮 TOUCH_END 根本不触发** —— 表现正是用户说的「有点击晃动反馈但没效果」。修复：**角标挂成 btn 的子节点**（坐标改按钮本地：AD_X=CELL_W/2-AD_ICON/2-2-BTN_X），事件从角标冒泡经过按钮 ✓。
- **无头验收两个新坑**：
  - **headless Chrome 的第一次 `Input.dispatchMouseEvent` 会被丢弃**（页面未激活）→ 症状是"第一次点必失效、第二次起正常"，极易误判成业务 bug。先发一次 `jsdrag`（mouseMoved 预热）再点即稳定。
  - **9333 调试口被上一次的 chrome 残留占用** → 三种死法：`chrome did not start` / handshake 403（连到旧实例）/ socket closed。`_cdp.py` 已加**第 9 参数 = 调试端口**（连跑多次换端口最省事），且异常时也写 `_cdp_log.txt`（含 `[EXCEPTION]` 行）、chrome stderr 落 `_chrome_err.txt`。
- **验证**：typecheck 0 错；构建 24s。CDP 实测：按钮中心/角标区/钱不够三条路径全部 hit s=1 e=1、sfx 含 "click"、数据正确购买；瓶子互不干扰（买 T2 不掉 T1、T1 配额 24 封顶）、商店含 sh:cursor(500,coin)、技能树瓶子模块无 p_cursor 行、文字 fs=52/scale=0.5（超采样生效）全部复验通过。

## 音频全量转 MP3（10:21）
- 13 个 wav → mp3：20.32MB → 1.40MB（6.9%）。bgm 96k 立体声 / win,buy,hit,slash 128k 立体声 / button 96k / click,click2,pop1-5 64k mono。
- 工具：`.workbuddy/tools/audio_conv.py`（lameenc，本机无 ffmpeg）+ `audio_verify.py`（MPEG 帧解析校验）；原 wav 备份 `.workbuddy/audio_src/`。
- 坑：lameenc 低码率悄悄降采样到 32kHz，必须 `set_out_sample_rate` 锁源采样率。
- 构建 web-mobile 27s 成功，13 个 mp3 全部进包；Cocos `getDuration()` 实测 116.61s 正常。
- 遗留：`build/vivo-mini-game` 是 9/21 旧产物仍含 wav，重打平台包时自然更新；BGM mp3 循环点有 ~0.04s 编码延时静音（mp3 固有），若用户反馈循环断裂再处理。

## 第二十轮补充：buy 音效只在商店购买时播一次（用户口径）
- **口径**：`buy` 音效只在**商店页**买到东西时补一声（金币 / 瓶盖 / 看广告补足三种付款都播且只播一次）；升级页、技能树的购买不播（只留统一点击 `click`）。
- **实现**：`RowSpec` 新增 `buySfx?: boolean`，只有商店四类行打开（shopBottleRows / machineRow / cursorRow / helperRow）。挂在 `UpgradeRows.makeCell` 的 TOUCH_END 里：`after()` 是调用方「确实购买成功」唯一会走的回调（满级/已拥有/满仓全是提前 return 不调它），所以 buy 放进 after 包装 + `bought` 防重复标志，同步付款与异步广告补足两条路径都覆盖。
- **验证**（CDP 实测，hook `Res.play` 记录音效名）：A 商店金币购买 `sfx=[click,buy,pop1]`；B 商店钱不够走广告 `sfx=[click,buy,win,click2]`；C 升级页买词条（money 100000→93862 实际成交）`sfx` 无 buy；D 技能树同理。typecheck 0 错，构建 21s。
- **⚠️ 新坑（无头验收）**：boot overlay（`#tb-boot`）在 headless 下可能**卡在「正在加载中」状态**（进度 rAF 动画不推进 → `shown` 永远 false → 它的 pointerdown 监听忽略一切点击）→ 之后所有 jsclick 全被这层吃掉，eval 却能正常读场景（假象：游戏正常）。**绕过：tpl 开头 `eval` 直接 `document.getElementById('tb-boot').parentNode.removeChild(...)` 强删**（`click:360,640` 点掉标题页的做法在卡加载态时无效）。

## 第二十轮复盘：图标/数字拉开 + 「新」角标移左上（用户两条）
- **问题**（截图放大定位）：① 货币图标与数字间隙只有 4px，长数字（1.00K）被 SHRINK 压到贴着图标，观感"图标挡数字"；② 「新」角标在右上角直接压住价格按钮的圆角。
- **改法**（全在 `UpgradeRows.ts` 常量）：`CUR_ICON_H 26→22`、`CUR_ICON_X 内缩 15→13`（图标右缘 -22）、`CUR_TEXT_X 10→13 / CUR_TEXT_W 48→50`（数字盒左缘 -12，间隙 ~10px）；「新」角标 `NEW_X = -CELL_W/2 + 25 - BTN_X`（左上角）、`NEW_Y = CELL_H/2 - 14`，尺寸 50×28→46×24，删 `NEW_X_AD` 让位逻辑（左上与右上广告角标天然不冲突）；`ICON_Y = -6`（瓶身图标下移 6px，瓶口只被角标盖 ~4px）。
- 验证：typecheck 0 错、构建 25s；seed 钱给少（300/200）让 on/off/ad/max 四态同屏截图确认。

## 贴图压缩（10:30）
- 11 张 >200K 的 png：Textures 目录 7.7MB → 2.6MB，已无单图 >200K。
- 6 张死资源移出条包（env/drum, fog, leaves, machine_box, paw_card, ui/wood_banner），5 条从 Res.ts TEXTURE_PATHS 摘掉；备份 .workbuddy/texture_src/unused/。
- 5 张在用大图量化 PNG8：wood_table 712K→39K、coin 510K→56K、icon_medal 340K→41K、icon_shop2 628K→72K、wood_banner_m 277K→19K。
- 构建 web-mobile 通过（26s）；包体 28M → 23M。剩余大头：NotoSansSC-Bold.ttf 10.5MB（字体子集化可再省 10MB）。

## 引导层（加载页 + 标题页）落地（~09:50-10:35）
- **需求**：用户给参考图（木桌+绿叶背景、「正在加载中」、黄瓶进度条）要求加加载页 + 点击开始的标题页（第二张参考图没传上来，标题页按同风格自设计）。
- **实现**：全部在 **`build-templates/web-mobile/`**（构建时覆盖产物，不用改引擎模板）：
  - `index.html`：完整复制生成版 + `#tb-boot` DOM 引导层（背景 `boot/loading_bg.jpg`、标题页 logo=`app_logo.png`、标题 `Bottle Flip Inc` + 点瓶子 + 点击屏幕开始）；进度条纯 CSS 画（无贴图），小瓶子 = `body_4.png`（PIL `ROTATE_90` 逆时针转 90° 变横躺、瓶盖朝右）。
  - 语言自适应：`navigator.language` 前缀 zh 判定中英文案。
  - **进度钩子**：`Res.loadAll` 三个 `resources.load` 的 onProgress 加权上报（tex 0.85 / aud 0.10 / fnt 0.05）→ `window.__tbBootProgress(f)`；`GameRoot.afterReady` 调 `window.__tbBootReady()`；引导层点击后调 `__tbOnStart()` → GameRoot 里 `music(false)+music(true)` **重播 BGM**（无用户手势时首次 music() 被浏览器自动播放策略静默拦掉）。
- **★ 关键教训：进度/标题切换绝不能用 rAF 循环驱动**。本机 headless Chrome 153 的 rAF 会**彻底停摆**（无 BeginFrame 就不推进），症状是进度条永远卡 96.93%、标题页不出现、`__tbBootReady` 明明已注册却「不生效」——排查绕了很久（一度怀疑 20MB wav / mp3 资源卡加载，实际 Res 早已全部载完）。最终改成：**样式写入只在上报时做一次，动画交给 CSS transition，切标题用 setInterval(120ms) 轮询**。真机浏览器 rAF 正常，但这样写两头都稳。
- **★ headless swiftshader 合成层伪影**：带 `transition/animation` 的 fixed 层截图会拿**陈旧纹理**（表现为「双进度条」「标题页下还挂着加载条」），DOM 求值却是正确状态。这是无头软件渲染的问题，真机没有；验证时以 eval 状态为准、截图只看布局。注入 `*{transition:none!important;animation:none!important}` 后截图即干净。
- **`_cdp.py` 两坑（已修/须知）**：
  - **第 3 参是本地 HTTP 服务端口**，会和 9333 调试口撞车（服务器先占 9333 → chrome 起不来 → `chrome did not start`，误导性极强）。验收时 HTTP 端口传 8901+，**别传 9333**。
  - `ThreadingTCPServer` backlog 默认 5 → Cocos 并发拉资源时报 `ERR_CONNECTION_REFUSED`/偶发 404。已把 `request_queue_size` 改 128。
  - 另：跑 `requests` 连 127.0.0.1 前要 `unset http_proxy...` + `export NO_PROXY=127.0.0.1,localhost`（沙箱代理 env + Windows 注册表系统代理都会劫持）；Bash 沙箱内 Chrome 子进程网络偶发丢请求，**验收跑 `dangerouslyDisableSandbox`** 稳定。
- **产物**：`build-templates/web-mobile/{index.html, boot/loading_bg.jpg, boot/bottle.png, boot/logo.png}`；包体 +244KB。验证：typecheck 0 错；构建 Finished；CDP 全流程 `cls=title → 点击 → gone=true → 游戏正常`（截图 `E:/LDC_Fby/_shots/g_title.png / g_game.png / crop_dbg.png`）。
- 与并行会话的 Res.ts 贴图清理（摘 5 条死路径，146 条）无冲突，共存验证 typecheck 0 错。


## 第二十一轮（续）：词条名阶前缀换瓶身图标
- 用户口径：「普通 收益」= 普通瓶子的收益 → 名字里的「普通·/铜瓶·」文字前缀**换成对应瓶子的贴图**（同比例缩放）。
- 实现：`RowSpec.nameIcon`（贴图路径，空则不画）；`makeCell` 建 `nameIc` Sprite（默认隐藏），`applyRow` 按 spec 切换 + 名字/说明两行**一起**右移收窄（`NAME_TEXT_X=-95 / NAME_TEXT_W=155`，右缘仍 60）。
  ⚠️ label 节点 contentSize 必须乘 `TEXT_SS`（栅格化尺寸），否则文字盒缩半 → SHRINK 蚂蚁字。
- `statRow()` 去掉 namePrefix 参数，`nameIcon: 'bottle/body_' + TIERS[tier].art`；商店/研发/模块行不带图标。`tierShortName` 现无调用点（保留备用）。
- 常量：`NAME_ICON_H=28`（瓶身 canvas≈0.4 → 宽 11）、`NAME_IC_X=-106`。与左上「新」角标（x -159..-113）不相交。
- 验收：升级页 6 行 / 技能树 bt2 树全部 `ic=ON:body_N`，非词条行不受影响；截图 `E:/LDC_Fby/_shots/t25_up.png / t25_tree.png / t25_cell_zoom.png`。typecheck 0 错，构建 25s。
- 坑：`showTreeCategory` 需要 `hasMachine`，验收 seed 别忘 `G.data.machine=true`，否则静默跳回商店页。
- ⚠️ 新坑（本机 Bash）：给 `python -c` 传含**反引号**的字符串会被 bash 当命令替换执行（把内容切碎、还把截图路径当命令跑）。写文件一律用 Write/Edit 工具，别用 bash heredoc/python -c 拼反引号文本。

### 第二十一轮（再续）：左侧图标改「稀有度底板 + 瓶身 + 右下角词条角标」（用户参考图口径）
- `RowSpec` 新增 `badge`（词条角标贴图）+ `badgeBg`（底板色=该阶稀有度色 `TIERS[tier].color`）；`statRow()` 的 `icon` 改 `'bottle/body_'+art`、`badge: d.icon`、去掉 nameIcon（左侧已有大瓶身）。
- `makeCell` 节点序=渲染序：`tierBg`(Graphics，先建) → `ic` 瓶身 → `badgeIc`(角标，最后)；applyRow 重画底板（Graphics 改色必须 clear+重描）、瓶身缩到 40 高、角标 setFrameH 18。
- 常量：`TIER_BG=46 / R=11 / TIER_BOTTLE_H=40 / BADGE_ICON=18 / BADGE_X,Y=-120,-24`；`ICON_X -138→-136`；名字/说明行右移统一由 `bd||ni` 驱动（`NAME_TEXT_X=-106 / W=166`，右缘仍 60）。
- 验收：升级页/技能树词条行 `bg=ON + 瓶身17×40 + badge=ON(income/cursor)`，非词条行不变；角标与说明文字间隙 5px 不重叠。截图 `E:/LDC_Fby/_shots/t25c_up.png / t25c_tree.png / t25d_cell.png`。typecheck 0 错。

## 版本上传（11:03）
- 提交 7a3168a「Optimize assets (audio to MP3, texture compression) and add boot/loading page」推送成功（91 文件，+1685/-1168）：mp3 音频、贴图压缩、死资源清理、build-templates 引导页、UpgradeRows/BottomPanel 改动、5 个新工具脚本。
- 用户确认**不上传** `.workbuddy/audio_src`、`.workbuddy/texture_src` 备份（25.7MB），已在 .gitignore 排除（原件仍在 git 历史里）。
- .gitignore 补 `.workbuddy/**/*.txt|*.log`（原规则只匹配一级目录）。
- 推送通道：沙箱默认代理这次 TLS 握手失败 → 改用系统代理 `-c http.proxy=http://127.0.0.1:10808`（v2rayN 已开）成功。**规律：先试沙箱默认，失败就切 10808。**

## 清理 chrome 泄漏 + 修预制体格式（12:55-13:10）
- **chrome 泄漏**：连跑无头验收残留 127 个进程 / 32 个 profile / 3.2GB。根因 = `_cdp.py` 的
  `finally` 只 `proc.kill()`，而 `--headless=new` 派生的 renderer/GPU/utility 子进程在 Windows
  上成孤儿后**不会自杀**。已修：新增 `kill_chrome_tree()`（taskkill /T /F + 按 user-data-dir
  兜底扫残留）+ `python _cdp.py cleanup` 一键清理入口。验收后实测残留 0。
- **生成的预制体打不开**（用户反馈）：对照参考工程 46 个 prefab 找出三条硬规则 ——
  ① 每个 Node 要 `_prefab`→`cc.PrefabInfo`（root→1、asset→0，DFS 后序）；
  ② 每个 Component 要 `__prefab`→`cc.CompPrefabInfo`；
  ③ **prefab 里 `_id` 必须是空串**（scene 里才是标准 uuid）。
  修 `gen_scene.py`（`_serialize(prefab_info=True)` + `_shift` 支持负数绝对下标占位 -1/-2），
  `ensure_meta` 改为补全且保留原 uuid、字段对齐编辑器真产物（ver 1.1.50 / files [".json"]）。
- 新增 `.workbuddy/tools/check_prefab.py`：对参考工程 46 个 prefab 全通过（校验器自检），
  本工程 Bottle/BottleShadow.prefab + Main.scene 全 OK。
- 编辑器侧闭环验证：`prefab_validate` valid=true、`prefab_edit{open}` 返回 `mode:"prefab-edit"`、
  hierarchy 读出 `Bottle→art`；`close{save:true}` 后 diff **只差 fileId 随机值**（字节级等价）。
  编辑器也成功打开 Main.scene（33 节点层级完整）。
- typecheck 源码 0 错；构建 0 个 "missing or invalid"；无头验收
  `{scene:Main, bootReady:true, bootGone:true, bottles:1, hasArt:true, scale:0.256, angle:180}` 零错误。

## 第二十三轮：UI 全面预制体化（UIRes 目录结构，参考 ZcVertical）
- **新框架**：`Scripts/Core/UIMgr.ts`（PageRoot/DialogRoot/TipRoot + showDialog/showPage/hideDialog，prefab 按需 resources.load + 缓存 + 并发去重）+ `Scripts/UI/UIBase.ts`（init/show/hide/close + popIn/popOut + Modal push/pop + maskNode 点击关闭）。
- **预制体**（`assets/resources/UIRes/<Name>/{Prefabs,Scripts}`）：SettingDialog / StatsDialog / AchDialog / OfflineDialog / ConfirmDialog / StartPage；公共行控件 ToggleRow / StepperRow / StatRow / AchRow / LocLabel 在 `UIRes/UIPublic/Scripts`。脚本组件用 `Script(cid)` 引用 + `comp.fields[...]` 回填 @property；数组型 @property（toggles/steppers/rows）直接塞 Comp 引用列表。
- **Main.scene** 加 PageRoot/DialogRoot/TipRoot 挂点（Widget 全拉伸），GameRoot 的 `__tb.ui` / offline / confirm 全部改走 UIMgr；StartPage 点击 → `Boot.I?.dismiss()` 揭掉加载层。
- **生成器**：`build_ui_prefabs.py`（shell=遮罩+九宫格面板+标题+关闭钮+ScrollView 内容）；编辑器序列化探针（execute_editor_script 建节点树 → `Editor.serialize` 导出 → 比对）标定了 ScrollView/Mask/Label/Graphics 的真实字段（`_N$layout` 指针数组、`_fontHandle`、outline 的 `_enableOutline/_outlineColor` 等）。
- **★ 硬坑（真金白银）**：
  - prefab 数组 @property 引用必须指向**组件**的 `__id__`，指到 cc.Node 上反序列化不报错、运行时 `row.bind is not a function`（toggles 踩过，steppers 对）。生成器追加组件 `tcomp` 而不是节点 `card`。
  - `_cdp.py` 模板里**普通 `eval:` 返回 [x,y] 不会点击**，必须 `jsclick:` 前缀 —— 写错是静默空操作，表现「点击没反应」极像业务 bug。
  - Label 的 `_string` 填空串会被编辑器 open→save 时按内容重算宽度（变 0）→ prefab 里直接填真实中文文案，运行时再由 LocLabel/脚本覆盖。
  - hideDialog 是异步退场（popOut 0.16s 后才 removeFromParent），验收脚本同步数 DialogRoot.children 会多数 1 个 —— 不是泄漏。
- **验收**（CDP 无头）：层级挂点 14 层全部 720x1280 正确；StartPage 按需加载 children=mask,Logo,SubTxt,TapTxt，jsclick 后 boot=gone/startPage=gone/bottles=3；SettingDialog toggles=3 bindFn=function steppers=3；StatsDialog overview=8；AchDialog rows=24；typecheck 0 错；构建 Finished=1 missing=0。
- **未完**：GamePage（HUD 顶栏/能力条/底栏/履带）仍是运行时 big-bang 生成（Hud.build/BottomPanel.build），任务 #16；底栏商店/技能树面板未预制体化；改动未提交 git。

## 第二十三轮补充：UIRes 目录合并 + 适配核验（用户两条）
- **用户诉求**：①预制体和脚本放一块 ②UI 模块与游戏模块分离。原来脚本误放在 `assets/UIRes/`、预制体在 `assets/resources/UIRes/`（分居两处，编辑器里找不到）。
- **迁移**：脚本全部并入 `assets/resources/UIRes/<Name>/{Prefabs,Scripts}`（与参考工程 ZcVertical 一致；prefab 必须在 resources 下才能按需 load），旧 `assets/UIRes` 删除。
  - ⚠️ **Cocos 目录 meta 在上级目录**：`mv UIRes/X/Scripts` 不会带上 `UIRes/X/Scripts.meta` → 必须单独补移，否则脚本 uuid 全变、prefab 里固化的组件 cid 全废。本次先 mv 目录后补 meta，scan_cid 验证 11 个类 cid 全不变。
  - import 深度修正：UIRes 内脚本 `../../../Scripts/` → `../../../../Scripts/`（多了一层 resources）；`../../UIPublic/Scripts/` 相对不变。
- **适配核验**（用户原则：背景拉伸 + 内容整体等比缩小）：代码早已实现且与原则一致 ——
  - `GameRoot.applySafeLayout`：`contentRoot.setScale(min(1,vh/1280))` 等比缩小（DOM 实测 0.947=1211.93/1280 ✓）；`bgDark/bgWood` 每次布局重设为 vwE+40/vhE+6 铺满（背景拉伸 ✓）；hudRoot/navRoot 按可见区上下沿钉位；worldLayer 按中部带 fit（min(fitH,fitW)）。分辨率策略 FIXED_WIDTH 720x1280。
  - StartPage/各弹窗在 contentRoot 树下，继承同一个 0.947 等比缩放。
- **⚠️ 截图不可信（再次坐实）**：两次独立无头运行产出**像素完全一致**的错误画面（StartPage 只剩背景、设置弹窗似被放大裁切），而 DOM 同时测得 Logo/SubTxt/TapTxt 全部 active 且在屏内（wp 360,802/578/331）、设置弹窗 frame ws=0.947、row 居中 wp=360 → **swiftshader 截图对 UI 层是确定性伪影，只能信 DOM 数值**。视觉确认要走编辑器预览或真机。
- 遗留：弹窗 prefab 内置遮罩固定 720x1280，s<1 时两侧各有 ~20px 盖不住（全局 Modal 遮罩有按 vwE+80 放大兜底，视觉差异极小）；GamePage（HUD/底栏）仍未预制体化（任务 #16）。

## 第二十四轮：分层 + 2K 分辨率 + 点击世界坐标（用户三条）
- **分层**：Main.scene 重构为 `Canvas → GameRoot → {gameRoot(游戏层), uiRoot(UI层), bootLayer}`。
  gameRoot：bgLayer + shakeHolder→worldLayer（bottles/hands/fx）；uiRoot：hudRoot/navRoot/adRoot/panelLayer/toastLayer/PageRoot/DialogRoot/TipRoot。
  **UI 不再随震屏晃**（旧结构 uiLayer 挂在 shakeHolder 下）。GameRoot @property：contentRoot/uiLayer → gameRoot/uiRoot。
- **2K 分辨率**：`DESIGN_W/H 720×1280 → 1440×2560`（用户口径「2K 倒过来」，主流手机竖屏）。
  引入**创作空间（author space）**：`DS=2`、`AUTHOR_W/H=720×1280`，全部布局常量/预制体仍按 720 口径书写，
  gameRoot/uiRoot 统一 `scale = sA×DS`（sA 按 720 算）——改分辨率零业务改动。
  `applySafeLayout` 全部数学换到创作空间（可见区÷DS；安全区÷DS÷sA）；worldLayer 钳制下限 0.50→0.32（横屏不再看不全）。
- **bootLayer**：改**设计单位**（常量×DS），不挂 Widget（超高屏可见区>设计区，Widget 会对齐到设计区）；
  `Boot.fitToVisible()` 把 bootLayer+Bg 拉伸到可见区 + BootCtl Widget updateAlignment；update() 监视可见区变化重排。
- **★ 点击错位根因**：`e.getUILocation()` 返回 **UI 坐标系**（原点=视口左下角），而 `convertToNodeSpaceAR` 要**世界坐标**，
  两者差 `view.getVisibleOrigin()`——旧代码假设恒等（vo=0 时碰巧成立，真机比例一偏就整体错位）。
  修复：`BottleField.uiToWorld()` 统一 `world = ui + getVisibleOrigin()`；`tapWorld(wx,wy)` 直接拿世界坐标喂 `Bottle.hitTest`
  （其内部就是 convertToNodeSpaceAR 世界→本地 + 瓶身矩形）。tapAt（field 本地中转）删除。
- **验收坑**：断言「点击命中」不能看 busy（flip 动画 0.44s，jsclick 的 pump 0.5s+sleep 后早回 idle）——
  要用持久计数 `G.data.stats.flips`（Save.ts stats 字段）。三视口（720×1280 / 412×915 / 1280×720 横屏）实测：
  gameRoot/uiRoot 世界缩放严格相等（1.894/1.845/0.556，等比✓）、bootLayer UT=可见区（背景拉伸✓）、**flips=1（点击全部真实命中）**。
  typecheck 0 错；构建 Finished=1 missing=0。
- 注：headless FIXED_WIDTH 下 vo 恒 0，错位修复无法在无头环境直接观测差异，但链路已按引擎语义改对。

## 从 GitHub 拉取当前版到 TapBottle_old（只拉不推）
- `git clone https://github.com/Mo-ldc/TapBottle.git .../TapBottle_old`：目标目录已存在但为空（git 允许克隆进空目录）。
- 结果：HEAD `7a3168a`（= 远程 main 最新）、493 个跟踪文件、working tree 干净；
  磁盘 50.66MB（.git 34.77MB + 工作区 15.88MB），4 秒完成。
- 网络：当天 v2rayN 开着，系统代理 `127.0.0.1:10808` 通（200）；沙箱代理由此**不可用（连接被拒）**。
  clone 与 push 同一套路：`unset HTTP_PROXY HTTPS_PROXY http_proxy https_proxy` + `-c http.proxy=http://127.0.0.1:10808`。
- 首轮要求「不要把现在的版本往上传」→ 当时确实未做任何 push（主工作区 218 个未提交的预制体化改动一律没动）。
- **第二轮：在 TapBottle_old 建 `legacy-runtime-ui` 分支存档「无预制体 + 程序生成 UI」版**
  （从 7a3168a 拉出，已 switch 过去并 `push -u` 到远程，GitHub 上现有 main / legacy-runtime-ui 两条分支）。
  选这个仓库的原因：主工作区已有 218 个未提交改动（预制体化：删 Main.scene、改 GameConfig/Res/Bottle/Theme…），
  old 是纯净的无预制体副本（无 `assets/Prefabs/`、无 `Core/Prefabs.ts`），拿它当存档分支互不干扰。

## 第二十五轮：适配层统一为「Widget 平铺 + AutoNodeScale」（用户口径）
- **口径**：把参考工程 `Init/Scripts/Tool/AutoNodeScale.ts` 搬来当**唯一适配组件**，
  编辑器实体化承担适配职责（不再运行时算尺寸），两边结构完全对称：
  - 游戏场景：`GameRoot`（gameRoot 的父）挂 `cc.Widget(45)` 平铺整个摄像机可见区；
    `gameRoot` UT=720×1280 + **AutoNodeScale** 等比缩放居中。
  - UI 预制体：根节点挂 `cc.Widget(45)` 跟随画布平铺；`mask` 挂 `cc.Widget(45)` 跟随父节点铺满；
    `fit` UT=720×1280（美术默认分辨率）+ **AutoNodeScale** 等比缩放居中。
- **新增** `assets/Scripts/Core/AutoNodeScale.ts`：参考工程原版 + `executeInEditMode(false)`
  （开着的话 prefab 编辑模式下父节点是编辑器视图，会把算出的 `_lscale` 写回 prefab 存档）。
- **★ 关键实测**：Cocos 3.8 的 `cc.Canvas` **不会**把节点 `UITransform` 改成可见区
  （引擎只管相机 orthoHeight），场景里写 1440×2560 就恒为 1440×2560 → GameRoot 的 Widget 铺 2560
  而不是真实可见高 → AutoNodeScale 算大（720×1280 视口实测 s=2，正确 1.8936，上下各多 ~68 单位被裁）。
  修法：`GameRoot.alignCanvas()` 里补 `Canvas.UITransform.setContentSize(可见区)`
  （参考工程是在 Canvas 节点上挂同样的 Widget(45)；本工程沿用"不挂 Widget、代码对齐"的历史选择）。
- **UIFit 退役**：6 个预制体里根节点的 `UIFit` 组件**原地改写**成 `cc.Widget`，职责拆成
  「根 Widget 平铺」+「fit 挂 AutoNodeScale」；`UIFit.ts` 保留但顶部标废弃。
- **生成器**：`.workbuddy/tools/fit_prefabs.py`（6 个 UI prefab）+ `fit_game_scene.py`（Game.scene），
  备份在 `.workbuddy/prefab_bak/`。
  **★ 改法要点**：只做「原地改写已有组件」+「数组末尾追加 [组件, 它的 CompPrefabInfo]」，
  **绝不删除/重排** —— `__id__` 就是数组下标，一重排就得全量 remap；末尾追加天然满足
  「组件与 CompPrefabInfo 相邻」，且不新增节点故每个节点的 PrefabInfo 后序位置不变。
  scene 里组件 `_id` 用标准 uuid（prefab 里才是空串）。
- **验收**（CDP 三视口，可见区 1440×2424 / 1440×2362 / 1440×712）：
  `gameRoot scale = fit scale = 1.8936 / 1.845 / 0.5563`，与改造前 `sA×DS` **逐位一致**；
  Canvas/GameRoot/uiRoot/DialogRoot/预制体根/mask 全部 = 可见区，720×1280 只出现在内容节点；
  点击 flips 0→2、money 0→5（命中链路未受 Canvas 尺寸改动影响）。typecheck 0 错、构建 Finished。
  编辑器 MCP 闭环：`prefab_edit{open}` → `mode:"prefab-edit"`；`scene_manage{open}`+hierarchy 读出
  `GameRoot[UITransform,GameRoot,Widget] / gameRoot[UITransform,AutoNodeScale] / uiRoot[Widget]` ✓
- **★ 验收坑（新）**：
  - `jsclick:` 坐标必须是**屏幕像素（原点左上角）**，不是世界坐标：
    `screenX=(wp.x-vo.x)*k`、`screenY=H-(wp.y-vo.y)*k`，`k=window.innerHeight/view.getVisibleSize().height`。
  - `_cdp.py` 窗口传 720×1280 时 `window.innerWidth/Height` 实测只有 **704×1185**（窗口框吃掉高度），
    可见区因此是 1440×2424 而非 2560 —— 排查适配偏差时先打这个数。
  - `F(scene,'bottles')` 命中的是**外层容器**（children = shadows/bottles/label），
    取瓶子要用 `__tb.field.I.bottles[0].node`。
  - `python` 命令没有 `requests`，跑 `_cdp.py` 要用 `C:/Users/A/.workbuddy/binaries/python/envs/default/Scripts/python.exe`；
    且 `_cdp.py` 第 4 参 WAIT 单位是**毫秒**（不是秒）。

## 第二十六轮：UI 资源按类型归位重组（用户拍板）
- **口径**：不再按「界面」分目录（旧 `UIRes/<Name>/{Prefabs,Scripts}`），改按**资源类型**归位 + 内部分类。
- **迁移**（工具 `.workbuddy/tools/reorg_ui.py`，备份 `.workbuddy/reorg_bak/`）：
  - 预制体：`UIRes/<Name>/Prefabs/<Name>.prefab` → **`resources/Prefabs/UI/<Name>.prefab`**（扁平）；
    `UIMgr.UI_ROOT` = `'Prefabs/UI'`，load 拼接改为 `UI_ROOT + '/' + key`。
  - 脚本：`UIRes/<Name>/Scripts/*.ts` → **`assets/Scripts/UI/`**（Pages/StartPage、Dialogs/5 个弹窗、
    Common/5 个行控件、_legacy/UIFit 退役件），import 相对路径脚本重算。
  - 贴图：`Textures/ui/` 88 张平铺 → **八分类** `nine/panel/button/icon/bar/deco/pixel/misc`；
    `Res.ts` TEXTURE_PATHS 88 条 + 调用方 22 处 `'ui/xxx'` key 全部带子目录重写。
  - 旧 `UIRes` 目录树 + 全部目录 meta 删除。
- **uuid 零变化**（18 文件级 + 88 贴图全部比对一致）→ prefab 内 SpriteFrame/脚本 cid 引用全部无需改动。
- **工具路径同步**：`fit_prefabs.py` / `build_ui_prefabs.py` 的 UIRes 常量 → `Prefabs/UI`；
  `_cdp.py` 的 `Log.entryAdded` 补打 `entry.url`（404 只有 text 没有 url 定位不到文件）。
- **验收**：typecheck 0 错、check_prefab 9 文件全过、构建 Finished 8s；
  产物 `resources/config.json` 路径表含 `Prefabs/UI/*` 6 条 + `Textures/ui/<sub>/*` 264 条；
  CDP：点击 flips 0→2，5 个弹窗 `showDialog→hasDialog=true` 全部加载成功，
  `StartPage load=OK`（GamePage FAIL 属正常 —— enum 预留项，prefab 从未实现，预载清单本就不含）；
  唯一 404 = favicon.ico（无害）。编辑器 `prefab_edit{open}` 新路径成功。

## 17:40-18:05 玩法 UI 场景实体化（第一阶段完成，因并行会话暂停）
- **TS 改造已落盘（typecheck 0 错）**：Hud/Abilities/CapMachine/BottomPanel/AdButtons 全部加了
  `bindScene()`（场景已有节点树→只绑引用+接事件）+ `construct()`（运行时兜底，与场景树逐节点同构）；
  原私有引用升级为 @property（moneyLb/capsLb/stam/bar/chipsNode/gateNode/binLb/tabShop/panel/content/scrollSV/hintLb 等）。
  UIKit 新增共用助手 `pressable()`（场景按钮统一接按压/click 音/回调）和 `applyFontDeep()`（场景 Label 换 NotoSansSC）。
  GameRoot.buildBackground/bindUI 改成 getComponent 优先。按钮节点命名约定：btn_back/chipCoin/chipCap/tool_*/stam(+fill)/
  berserkLb/ab_*(+cd+lb)/railTop/binLb/chips/tab_shop(+label+tabNew)/dropdown(+ddLb+newTag)/panel(+hintLb+msLb+scroll)/ad_*(+dim+label)。
- **编辑器 MCP 探明**：Game.scene 已打开（uuid 8f1c…4e51）；GameRoot @property 全绑好（resRoot 空属正常）；
  ⚠️ 场景进程里 `cc.resources.load` 不可用（"Can not parse this input"）→ 编辑器内现载贴图不可行，
  改走 gen_scene.py 直写 JSON（工具链完备：Node/Sprite/Label/Script/ScrollView + scan_cid 的压缩 cid）。
- **发现并行会话**：17:54:32 全部脚本被外部批量重写（贴图按 bar/button/deco/icon/misc/nine/panel/pixel 子目录重组、
  资源路径同步改 ui/nine/nine_base 等），与我的 TS 编辑合并后 typecheck 仍 0 错。用户确认有并行 → 暂停后续场景搭建。
- **续作要点**：下一步=写 build_gameplay_ui.py（import gen_scene，向现有 Game.scene 追加 6 棵子树：
  bgLayer 背景/hudRoot 顶栏/abilityBar/belt 履带外观/bottomPanel 底栏骨架/adRoot 广告牌；
  Graphics 画的部件（belt 面/滚筒/roundedPanel/箭头/播放标）在场景版改用九宫格+需新贴图
  tri_down/tri_play/belt_face/feeder_roll（PIL 生成）；card_white meta 补 borderSlice 26）→
  scene 兜底校验 → typecheck/构建/_cdp 验收 → commit。

## 第二十六轮补充：Scripts/UI 内部再分类（用户要求）
- **结构**（依赖严格单向：Base ← Widgets ← Hud/Panels ← Pages/Dialogs）：
  - `UI/Base/`：UIBase / UIKit / Theme / Modal / Toast / Ads（基础设施，Ads 因被 Dialogs 跨层引用放这）
  - `UI/Widgets/`：AdButtons / UpgradeRows + 原 Common/ 的 5 个行控件（Common 目录已删）
  - `UI/Hud/`：Hud / BottomPanel / Guidance
  - `UI/Panels/`：Panel / AchPanel / SettingsPanel / StatsPanel（内嵌面板体系）
  - `UI/Pages/ Dialogs/ _legacy/` 不动
- **工具** `.workbuddy/tools/reorg_ui_scripts.py`：import 重写用「解析 import 绝对目标 → 按新位置重算相对路径」，
  **全量扫 assets/Scripts**（连未移动的 GameRoot.ts / Game/*.ts / Core/UIMgr.ts 都要改，因为目标跑了）。
  ⚠️ 新位置目录必须取 `new_path_of(cur_rel)` 的 dirname——第一版误用当前目录，同目录互引被算成 `./Base/X`（干跑抓出已修）。
- **uuid 零变化 → prefab 内 20 个脚本 cid 全部原样识别**（`_cid_map.json` 是 name→cid，查方向别反）。
- **验收**：typecheck 0 错、构建 Finished 33s、CDP：flips 0→2、6 预制体全缓存、
  5 弹窗 `getUI()` 返回正确组件类（cid 链路完好）、`bottomPanel=OK`。
  headless 截图只有桌面+瓶子是 swiftshader 既有伪影（与上轮 fit_click.png 一致），弹窗截图渲染正常。
- skill 文档同步：project-map.md / cookbook.md（面板示例改 Panels/ + ../Base/ 导入）/ pitfalls.md 路径。

## 相机视野 ≠ 设计分辨率（诊断，未改代码）
- 现象：摄像机看到的内容不是 1440×2560。定性：FIXED_WIDTH 正常行为，非 bug。
- 实测（_cdp 8902/9355，窗口 720×1280）：vis=1440×2423.86，des=1440×2560，innerW/H=704×1185。
- 可见高度=1440×(屏高/屏宽)，随宽高比浮动；9:16 才等于 2560。排查时先打 getVisibleSize()。

## 18:08-19:00 玩法 UI 场景实体化完成（接续暂停段）
- **build_gameplay_ui.py（新工具）**：向现有 Game.scene **追加** 5 棵子树（580 对象，只追加不重排）：
  hudRoot 顶栏 12 节点 / navRoot abilityBar / belt 履带机器 8 节点 / bottomPanel 5 节点 / adRoot 3 节点。
  节点树与各组件 construct() 逐节点同构；节点名必须对上 bindScene 的查找约定。
  Graphics 画的部分在场景版全部换成 Sprite 组合：woodRail 描边→nine_stroke、beltFace→px_white2+slat 子节点、
  feeder 滚轮→px_white2+slats、chevron/播放三角→新贴图 ui/misc/tri_down(28×16)/tri_play(10×12)（手写 image meta，格式照抄 px_white2）。
  card_white.png.meta 补 borderSlice 26（场景 SLICED 需要 meta 有边距，运行时 sliced() 只在兜底路径补）。
  ⚠️ append 技巧：_serialize([holder])[1:] 后，子树根 = _parent 仍指向被丢弃 holder 的节点（shift_ids 跳过负数→恒为 -1）。
- **★ 引擎大坑（真金白银）**：场景反序列化的 Sprite/Label **全部不渲染**——节点树/组件/贴图/颜色/
  世界变换逐项探针全部正常（spriteFrame 在运行时都解析到了），就是不画；运行时 addChild 的新节点正常；
  重建组件、重新激活、markForUpdateRenderData 全都无效。根因：场景被 preload 提前实例化的时序下，
  首次 markForUpdateRenderData 发生在渲染管线就绪前 → 请求被丢弃且永不重发（dirtyVersion 停在 5）。
  **修复 = 把子树摘下来重新 addChild**（_lpos/siblingIndex 不动）→ 重新走 onEnable 立即恢复。
  已在 GameRoot.rebindSceneRenderers() 固化：afterReady 时重挂 hudRoot/navRoot/adRoot 三层。
  排查耗时极长（12 轮无头探针），下次任何"场景 UI 摆好但不渲染"直接套这个修复。
- **Hud.bindScene 笔误修复**：moneyLb/capsLb 兼容两种摆法（chipCoin 子节点 或 hudRoot 直接子节点，
  construct() 建的是后者）——之前按 chipCoin/moneyLb 找 → null → update() 每帧 TypeError。
- **BottomPanel.bindScene** 补挂 UIScrollBar（场景只摆 sbar/thumb 节点，组件进场景后在运行时接线）。
- 验收：typecheck 0 错；web-mobile 构建；无头截图整屏 UI 正确（顶栏/广告牌/底栏页签/商店列表全渲染，
  履带正确隐藏=未买机器）；jsdrag 预热后 jsclick 点瓶子翻转成功；控制台 0 异常。
- 场景备份：.workbuddy/scene_bak/Game.scene.pre-gameplay-ui.json。
