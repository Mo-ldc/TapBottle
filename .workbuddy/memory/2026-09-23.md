# 2026-09-23

- 将游戏完整实现提交并推送到 GitHub（提交 3fa0bd8，459 个文件：脚本、贴图/音频/字体资源、GDD、extensions/cocos-creator-mcp、.workbuddy 工具日志）。用户确认 .workbuddy 一并上传。
- 网络排障：当天 v2rayN 未运行，系统代理被禁用，直连超时；最终发现沙箱默认代理（env 注入的 127.0.0.1:58048，git 不加任何 -c proxy）这次能通 GitHub——两天前的 502 是瞬时的。结论：推 GitHub 时优先试默认沙箱代理，不行再切 127.0.0.1:10808（需 v2rayN 开着）。
- 补交 README.md（项目介绍 + 目录结构 + 更新日志，提交 2b7b6a3），此后仓库首页不再空白。
- 用 GCM 存的 OAuth token + GitHub API（PATCH /repos/Mo-ldc/TapBottle）设置了仓库 Description。要点：`git credential fill` 可取回 token；PowerShell 5.1 发中文 JSON body 必须 `UTF8.GetBytes` 转字节，否则 400。
- **产出差距分析**：`差距分析_vs_原版方案v1.1.md`（工程根）。基准 = 参考目录里 79KB 的 GDD v1.1（原版二进制真实数值）+ `Bottle_Flip_Inc_全量资源导出`。结论：玩法骨架已齐（三态/双货币/7 阶/4 分支技能树/履带+闸门/助手/三终极技能/24 成就/离线/双语/四比例自适应），**差的是数值（几乎全是自造）与 6 个整块系统**（里程碑 24 阶 / 商店 Modal / 11 词条缺 4 项 / 助手 6 张阶数许可 / 随机连锁 / 完成度通关），外加「判定用 RNG 而非角度」导致手感不可练。已把 v1.1 拷进工程为 `GDD_Bottle_Flip_Inc_Cocos_v1.1.md`（v1.0 仍在，待用户决定是否替换）。
- **UI 动效改造（用户要求：不要从下方出现、Q 弹中间出现、隐藏是缩放回去）**：UIKit 新增 Q 弹 `popIn`(0.72→1.07→1)、`popOut`(1→0.78+淡出)、`maskIn/maskOut`、`clearPopTween`；**Drawer 由底部抽屉改为居中模态卡**（全屏遮罩+BlockInputEvents 卡片，drawerH 600→820）；Panel/离线弹窗/删档确认/Toast 全部收敛；`MASK_SIZE` 改为按可见设计区动态值；面板层不再跟随 navDY，打开时保留底栏。
- 验收：typecheck 源码 0 错 → 构建 web-mobile 成功（16.2s）→ 新写 `E:\LDC_Fby\_testui.tpl` + `_runui.py` 四比例跑批。采样到 `cardUI` 缩放曲线 `0.720 → 1.104 → 1.000`（Q 弹过冲证实）；关闭后截图与基线**字节数完全相同**，证明遮罩/卡片确实收干净了。
- 踩坑：跑批脚本里 `window.cc` 没有 `cc.UITransform`/`cc.UIOpacity`（要用字符串 `getComponent('cc.UITransform')`）；`__tb.drawer` 是类、实例是 `Drawer.I`；`___TAG___` 占位两侧的下划线会被 replace 吃掉（截图名变成 `uip161.png` 之类）。
- **用户反馈「两张巨大的图挡住游戏」** → 定位到两个东西：
  ① **狂暴火焰** `Abilities.berserkFx` = 220×290 摆在 `(-230, navY+250=-234)`，直接压在桌面左下 + 快捷购买卡上；
  ② **光标**：`BottleField` 把 `env/cursor` 这张手指图直接 `setSize(r*2)`（r = 70×cursorSize，最大 308px）当「翻瓶范围」用 → 一只巨手糊在桌面中间。
  另外 `samuraiFx` 是 700×900 的武士刀铺满屏幕（同一类问题，一并收掉）。
- 修法：① 状态特效挂 `navRoot` + `setSiblingIndex(0)`（UI 永远在它上面）或挂 `abilityBar` 且排在按钮之下，尺寸收到按钮外一圈（火焰 176×224 / 刀 160×184），位置每帧跟随按钮；
  ② 光标拆两层：`ring`（`env/areacircle` 半透明 55、随升级放大）+ `pin`（固定 60px 手指），容器 `setSiblingIndex(0)` 压到瓶子层之下当地贴；顺带加 `Modal.open` 时隐藏。
- 验证：新增 `E:\LDC_Fby\_testfx2.tpl` + `_runfx2.py`（种状态 `p_cursor/p_cursorsize=6/a_berserk/a_samurai/h_unlock` + `berserkFlips=25` + `samuraiTimer=8` 再截图 + dump 节点）。
  实测 `berserkFx@(0,-372) 176x224 sib=0`、`cursor 241x241`、`pin 60x60`，p16/tall 两比例桌面与瓶子无遮挡。
- 构建假失败：这两轮 `CocosCreator.exe --build` 返回 **exit 36**，日志里 `Error: Exit process with code:null, signal:SIGTERM in task build-script`，但同一份日志结尾是 `build Task (web-mobile) Finished in (15 s)`，且产物里能 grep 到新代码 → **看 exit code 会误判**。本机有个用户手动开的 Cocos 编辑器会话占着 `127.0.0.1:3000`（MCP），命令行构建与它撞车就会报这个 SIGTERM。
- **用户指出「科技树 / 瓶子升级的内容和原游戏对不上」，并要求按原版实现「瓶子各自的悬停触发」**（未解锁该阶悬停 → 只能点击；解锁后手指圈内触发该阶瓶子）。本轮按 v1.1 真实数据把**数值层整体换血**：
  - `GameConfig.ts`：TIERS（$1→$2500 / ×1→×7 / 科技 1600~90000 盖 / 商店 $7~$2500 / 上限全 30 / 悬停 $100~$230000 / 扣盖 5.0 铜瓶 6.0 / mass）、
    BOTTLE_STATS（**11 词条 + 悬停解锁 = 12 行**，每行 `tiers[7]` 真实 base/max/r/step）、SKILLS（瓶子 6 / 玩家 13 / 助手 12 / 技能 9）、新增 FLIP 常量、SAVE_VERSION 5。
  - `State.ts`：重写派生（bottleIncome 走 §4.3 公式、tierTolerance 容差角、tierCap 30+5L、capUnit 机器产出、handCost 4000×1.10ᴺ、maxHands 10+5L、cokeCd 75−20L、berserkFlips 2+L、berserkMult 1.0+1.5L、samurai 0.8+0.3L / 1.0+0.3L）；
    新增 `rollLanding/outcomeOf`（角度制判定）、`hoverUnlocked/hoverable/anyHover/buyHover`、`tierResearched/UNLOCK_SKILL/HELPER_PERMIT`、`helperAllowed`。
  - `Save.ts`：版本不符时只重置 tierStats + skills（索引/id 空间重排过）。
  - `BottleField.ts`：光标半径改 `G.cursorRadius`（55~82.5px）、圈内只触发 `G.hoverable(tier)` 的瓶子、随机连锁翻转走 `tierRandomChance`。
  - `HelperHands.ts`：只抓 `G.helperAllowed(tier)` 的瓶子，节奏按 `G.handInterval` + `handSpeed/speedBase`。
  - `SkillPanel.ts`：**四个页签**（瓶子/玩家/助手/特殊技能）对应四条真实分支；页签改 152 宽 ×4。
  - `DrawerContent.ts`：词条行支持金币/瓶盖双货币（C 前缀）、未研发阶数锁按钮、容差角展示、助手页显示已覆盖阶数。
  - `Locale.ts`：新增 ~40 条 `bs_*` / `sk_*` 文案（含 T2~T7 研发、六张自动化许可、稳定性/机器/闸门等），并清掉两条重复 key。
- 验收：typecheck 0 错 → 构建成功（16s，产物 grep 到 ul_t2/tree_page_helper/hoverable/rollLanding）→ `_runv11.py p16` 截图 6 张 + 数值 dump 与 §4.1/§4.2 **逐项一致**；`_testhover.tpl` 五阶段验证悬停流程：
  A 未解锁→圈不出现 flips=0 → B 瞄准但未解锁该阶 flips=0 → C 解锁 T1 后 flips=2（T1 标 `*`）→ D 瞄准 T2（未解锁）flips 不变 → E 解锁 T2 后 flips=7。
- GDD 自身矛盾：§4.5 举例（Income 10 / Speed 80 / Mastery 300…）与 §4.2 矩阵（40 / 35 / 50）冲突，一律以 §4.2 为准；T4~T7 多行 `$10/R1.50` 疑似反编译填充值，按原文保留待复核。
- 用户要求给"Add game implementation"提交补 Description（提交正文）：detach 到 3fa0bd8 → `commit --amend -F msg.txt` → cherry-pick README 提交 → `branch -f main` → `push --force-with-lease`。新历史：ec9d485（带正文）→ 563189d（README）。
- 注意：工作区还有未提交改动 `assets/Scripts/UI/Panel.ts`、`UIKit.ts`，以及未跟踪的 `.creator/asset-template/`、`.workbuddy/memory/2026-09-23.md`。
- v1.1 对齐版已提交推送（931206e，28 文件 +3884/-625）：真实数值 GameConfig、角度制三态判定、SAVE_VERSION 5 迁移、GDD v1.1 与差距分析文档、gap_scan.py。`.gitignore` 新增 `.workbuddy/*.txt`、`.workbuddy/*.log`（排除一次性排查产物）。

## 第三轮：用户「文本层级貌似被挡住了」→ 面板串层 / 列表底部截断

定位过程（都是先量化再改）：
- 把 `v11_p16_4.png` 面板内部左上角放大 3 倍，**能看到背后 HUD 的设置/奖杯图标和金色金额数字透出来**；逐行取样证明是 +6~10 灰阶的鬼影。
  根因：`Panel.frame` 底色 `#171E2BF7`（alpha 247 = 96.9%），剩下 3% 就是穿透量。`Drawer.bg #141A24F7`、离线弹窗 `#1B2230F5`、删档确认 `#1B2230F7` 同病。
- 用探针（`_testgz.tpl`）量到 ScrollView 的几何：`view` 高 620、`info` 顶边距 view 下沿只有 12px；瓶子页节点中心间距 124，节点中心距内容顶 **140**，而 `620 mod 124 = 0` → **视口下沿永远切在节点中心下方 16px —— 正好从名称文字中间划过去**。
- 升级面板词条行：`商店购买价格增长 0% → -1%（每级压低 1%）` 在 300px 描述框里把结尾的「）」挤到第二行当孤字；行高 104 / 行距 110，名称(y=32 顶到 48)与按钮(y=16~-52)贴死，按钮底正好压在卡片下沿；Drawer 的滚动区下沿又与卡片金色描边重合 → 最后一行被描边切掉半个。

改动：
- `UIKit.modalMask` 遮罩 alpha 170→200；`Panel.frame` → `#171E2B`、`Drawer.bg` → `#141A24`、离线弹窗/删档确认 → 全不透明。**模态面板一律实心底**。
- `UIKit.scrollView` 新增 `UIScrollBar` 组件 + 指示条（track 6px 白 alpha28，thumb 6px 金 `#C8A44A`），挂在 `parent`（视口外）不参与滚动、不被遮罩裁；每帧只比对 content 高度与 offset，变了才重排。
- `SkillPanel`：`SCROLL_H 620→574`、`SCROLL_Y -10→-15`（口径：`SCROLL_H ≡ padTop + NODE_SIZE/2 + STEP_Y/2 = 202 ≡ 78 (mod 124)`）；初始 offset `round(off/STEP_Y)*STEP_Y`；节点三级文本重排（图标 44@28 / 名称 30@-16 / 状态 22@-42，零重叠，原来图标底 7px 压在名称上）；信息条名字把 `\n` 换成空格。
- `GameConfig`：`NODE_STEP_X 120→130`（列缝 8→18px）。
- `Locale`：所有节点名压到 ≤6 字（助手六张许可 → 「铜瓶许可」等；`范围购买上限提升`→`购买上限提升`…），T2~T7 研发名改成两行 `研发 T2\n铜质能量瓶`。
- `DrawerContent.makeStatRow`：行高 104→118、行距 110→128、描述框 320×54（3 行，尺寸加大后长句单行排满，孤字消失）、按钮下移不压边；`Drawer`：`BOTTOM_PAD 28`，滚动区下沿从卡片底边收到卡片底边上方 28px。
- 验收：typecheck 0 错 → 构建成功（17s）→ `_testfix.tpl` / `_testfix2.tpl` 两比例跑批。几何探针 `viewBottom=331 infoTop=301`（30px 缝）、`sbarTrackH=664 thumbAct=true`；放大对比图证实鬼影彻底消失、五行许可名单行不挤、底部一行完整可见。
- 踩坑：跑批脚本的收尾截图会覆盖我传给 `_cdp.py` 的那个输出路径 → 首张截图要用别的文件名，否则被终态覆盖。`tail`/`dirname` 不可用，`&&` 链会整条断掉（曾导致构建根本没跑但 echo 出 exit=127）。

## 第四轮：用户「解锁光标后，图片不在上层、被挡住了、没出现在手指点击处」

先做**不依赖输入的探针**（`_testcur.tpl`）排除坐标映射嫌疑：
- `ut.convertToNodeSpaceAR(field.node.getWorldPosition())` = `(0,0)` → 映射本身是对的；
  本工程 UI 世界原点 = 可见设计区左下角，所以 `getUILocation()` 与 `convertToNodeSpaceAR` 的入参**是同一个空间**，不需要加减半屏。
- 探针同时暴露真正的问题：`kids=[cursor,shadows,bottles,label]`、`cursor sib=0` —— 光标容器**整块**被压到了影子层/瓶子层之下。
- `f.pointer` 只在 `input TOUCH_MOVE` 里更新 → 「只点不拖」时永远停在初值。

三个成因与修法：
1. **层级**：光标拆成两个独立节点 —— `cursorRing`（范围圈，`setSiblingIndex(0)` 当地面贴片，压在影子/瓶子之下）+ `cursorPin`（手指，追加到子节点末尾 = 最上层）。原来共用一个容器、整容器 sib=0，手指跟着一起沉底。
2. **跟随**：`TOUCH_START` 也要取坐标，而且**桌面浏览器用鼠标时按下派发的是 `MOUSE_DOWN` 而不是 `TOUCH_START`**（实测：只点不拖时 pointer 纹丝不动，拖动才动 → 拖动的更新其实来自我新加的 `MOUSE_MOVE`）。
   现在四路都接：节点级 `TOUCH_START/MOVE` + `input TOUCH_START/MOVE` + `input MOUSE_DOWN/MOUSE_MOVE`，统一走 `aim(uiX, uiY)`。
   注意：`Bottle.enableTouch` 在 TOUCH_START 里 `propagationStopped = true`，所以**点在瓶子上时节点级 TOUCH_START 不会冒泡到 field**，必须靠 `input` 级的 MOUSE_DOWN / TOUCH_START 兜住。
3. **指尖精准落点**：用 PIL 量 `env/cursor.png`（73×78）最上面 6 行不透明像素的重心 → 指尖在贴图中心 `(−0.048, +0.487)` 比例处；于是贴图中心要放 `pointer + (−TIP_RX*W, −TIP_RY*H)` ≈ `(+2.7, −29.2)`。原来摆在 `(0,-6)`，指尖比触摸点高了约 23px。
   另：给手指加了一层右下偏移 3~4px 的黑影（`#101010`、opacity 95）—— 瓶子也是粗黑描边，手指直接叠上去会糊成一团。
4. 首次可用时把 `pointer` 摆到 `PLAY_AREA` 中心（`pointerSeeded`），否则停在节点原点看着像「没出现在该在的地方」。

验收（`_cdp.py` 新增 `drag:` / `jsdrag:` 指令，用真实 mousePressed→mouseMoved×8→mouseReleased 驱动）：
`A 初值 pointer=(30,65)+环激活+ring sib=0/pin sib=4` → `B 单击后 expect=(-70.4,78.8) vs pointer=(-71.1,79.1)` → `C 拖动后 expect=(24.8,-92.1) vs pointer=(24.3,-91.4)` → `D 关掉 p_cursor 后 ring/pin 都 act=false`。p16 与 tall 两比例一致，误差 <1px。

★ **易错**：`cursorPin` 的投影必须做成它自己的**子节点**且排在手指前面 —— Cocos UI 渲染是深度优先，同一节点自己的 Sprite **先画**、子节点后画；把投影做成 pin 自身 Sprite 的兄弟会盖在手指上（实测整只手被染成灰色）。

### 附带修掉：一次点击翻两只瓶子（`_testdbl.tpl` 实测 flips 0→2→4）

- 现象：点一只瓶子，`G.data.stats.flips` 每次 +2。
- 成因：`Bottle.enableTouch` 在 **TOUCH_START** 翻那只瓶子并 `propagationStopped = true`；
  而场地的「空白处点击 → 翻转随机瓶子」挂在 **TOUCH_END**（没有那层拦截），事件照样冒泡上来 → 再随机翻一只。
- 修法：场地那条改成挂 **TOUCH_START** —— 点在瓶子上时事件已被 `propagationStopped` 截断，走不进来；
  只有真正的空白处按下才会触发。改完实测 `flips 0→1→2`（每次点击正好一只），货币增量也对上了。
- 顺带好处：响应从「抬起手」提前到「按下」，手感更跟手。

## 第五轮：用户给原版「开局流程」截图，逐条对齐

用户描述（配两张原版截图）：开局桌台下方**没有传送带**；在右边的**商店**花 **$1,000** 买下「解锁瓶盖机器」之后才有，而且**只有装了机器才会获得瓶盖**；普通瓶初始上限 30、默认给 1 只；**升级界面开局只有 2 条**（瓶子收入 / 悬停）；悬停是每种瓶子各一个一次性选项，不买只能点击，买了之后 PC 放鼠标、手机放手指就触发；**手指圈是触发范围，随「范围」解锁才有，初始没有圈**。

逐条核对与改动：
1. **瓶盖机器从技能树搬到商店，改价 $1,000 金币**（原来 `p_machine` 是技能树节点、1,000 **瓶盖**）。
   - `GameConfig`：删掉 `p_machine` 技能节点（`unlockId` 只是元数据，无逻辑读取，删了不影响），`p_machineinc` 直接挂到 `p_stability` 下；`MACHINE.buyPrice = 1000`。
   - `Save`：新增 `machine: 0|1` 字段；**迁移**：旧档若 `skills.p_machine` 有值则补 `machine=1`（不让玩家丢东西）。
   - `State`：`hasMachine` 改读 `data.machine`；新增 `buyMachine()`（金币够才扣）。
   - `DrawerContent`：瓶子升级页新增「瓶盖机器」设施卡（未买显示 $1,000 + 购买、已买显示「已安装 · 传送带运转中」，一次性价格用千分位原样显示，与截图 $1,000 一致）。
2. **未解锁时传送带整块不存在**：`CapMachine.refreshLock()` 直接切 `this.node.active = on`（不再是「变暗 + 未解锁提示条」）。
   ⚠️ 关键点：node.active=false 后 `update()` 不跑 → 必须靠 `G.addListener(() => this.refreshLock())` 在买下机器（会 notify）时把自己叫醒。
3. **没装机器就不产出瓶盖**：`doFlipResult` 里 `if (!this.hasMachine) { caps = 0; }`，并去掉「直接入账」分支（装了机器才进 `pendingCaps`）。
4. **初始 1 只 + 上限 30**：核对 `defaultSave().bottles = [1,0,...]`、`tierCap(0) = 30 + limit*5 + p_sizelimit*5` ✓ 已符合，无需改。
5. **手指圈门槛**：`BottleField` 改成 `G.hasCursor && !Modal.open`（去掉 `anyHover` 一票否决）——圈只是触发范围指示灯，圈内瓶子会不会自动翻取决于那阶有没有买悬停。
6. **顺手修掉一个数值 bug**：`bottleCost` 原来 `k = n+1`（多乘一次 R），T1 拥有 1 只时显示 **$9**；用户截图是 **$7**。
   校准为「按**已购买次数**算，T1 自带那只不计数」→ `bought = max(0, n - (tier===0?1:0))`，T1 n=1 → $7 ✓，T2 首次购买 = base $2,000 ✓（与 GDD §4.1「商店第2瓶 $7」一致）。

验收（`_testmach.tpl`，p16 + tall 两比例）：
`T0 machine=0 capsNode=false money=0 b0=1 capLimit=30 buyPrice=7` →
`T1 未装机器的扣盖 caps=0 deferred=false` → `T3 金币999买=false` → `T4 金币1500买=true money=500` →
`T5 machine=1 capsNode=true` → `T6 装机器的扣盖 caps=1 deferred=true` → `T7 pending=1`（在履带上运输）。
截图确认：开局桌台下方干净无履带、商店卡显示 $7 + 瓶盖机器 $1,000（不可买时按钮灰）、买后显示「已安装」。

**待用户确认**：① 升级页开局只显示「收入 / 悬停」两条，其余 9 条靠什么解锁（GDD §7 的 24 里程碑里有「解锁商店初级升级」，疑似里程碑门控）？② 顺序（原版截图是「收入」在上、「悬停」在下；我们目前悬停置顶）？③ 面板要不要拆成「商店 / 升级」两页签（截图里是）？

## 第六轮：用户「金币文本和效果文本还在背景的后面」

一句话定位：**`fxLayer`（所有飘字的老家）在世界层的兄弟序是 0，一直压在桌面之下**。

证据（探针 `_testfxz.tpl`）：
`worldLayer.children = fxLayer#0, table#1, bottles#2, hands#3, caps#4` → `fxLayer sib=0`。
我在桌子正中间 spawn 了 `+$5` / `扣盖` / `MISS` 三个飘字，截图里**一个都看不到**（全部被桌面挡住）。

成因（典型的「建得早 = 画得早」错位）：
`fxLayer` 是在 `GameRoot.onLoad()` 里挂到 `worldLayer` 的（挂世界层是为了和世界坐标一起缩放/平移对齐），
而 `table / bottles / hands / caps` 是后面 `buildWorld()` 才建的 —— 后加的排在后面（画在上面），
于是后建的桌面把先建的特效层整个盖住。

修法：`buildWorld()` 末尾 `this.fxLayer.setSiblingIndex(this.worldLayer.children.length - 1);`
（仍在 `shakeHolder` 内，坐标一致性不受影响）

验收：
- 修后 `worldLayer.children = table#0, bottles#1, hands#2, caps#3, fxLayer#4`，三个测试飘字全部浮在桌面上；
- 真实翻转抓帧：`扣盖 $5`（金色）明确压在桌面木纹之上（`_shots/crop/fxw_hit_2.png`）；
- p16 / 9:20 两比例一致。
- 附带确认：飘字时长 0.85s、`fadeStart=0.42` → **只有落地后 0.36s 内是满不透明**，之后开始淡出，
  所以抓帧要卡在落地后 0.2~0.4s（抓晚了会以为「没渲染」，我第一轮就被这个骗了）。

## 第八轮：完成「升级页开局只显示 2 条」——GDD §7 里程碑 24 阶系统 + 词条门控

**用户继续要求完成未完成任务**：核心就是回答第五轮末尾的「升级页开局只有 2 条，其余怎么解锁」。

**实现内容**：
1. **里程碑 24 阶系统**（`GameConfig.ts`）：落地 GDD §7.2 原版 `MilestoneSO` 真实阈值表（10 → 1,100,974）。
   - 进度值 = `max(累计翻转次数, 累计收益 = 金币流水 + 瓶盖流水)`，用流水而不是余额，避免花钱倒退。
   - `State.ts` 新增 `milestone / msNext / msRatio / statUnlocked / statUnlockedCount / syncMilestones / onMilestone`。
   - 达成新阶段时 `G.onMilestone` → GameRoot 弹 Toast「里程碑 Mx 达成 · 解锁词条 xxx」+ 全屏金色闪光。
2. **单瓶词条门控**（`DrawerContent.ts`）：
   - 开局只显示「金币基础收益 + 悬停翻转」两条；其余 10 条按里程碑阶段（M1..M13）逐条解锁。
   - 显示顺序调整：收入在上、悬停在下，中间按解锁顺序排列（`BOTTLE_STAT_VIEW`，与存档下标解耦）。
   - 未解锁提示：分区标题显示「瓶子词条 n/12」和「下一阶段 Mx」。
3. **固定里程碑条**（`Drawer.ts`）：把进度条移到抽屉头部下方、不随滚动内容走，避免占用滚动区把词条推出屏幕。
   - 显示：当前阶段号、进度 / 下一阶阈值、进度条；瓶子升级页签出现，助手页签隐藏。
4. **统计面板**（`StatsPanel.ts`）加里程碑行：当前阶段 + 下一阶段进度。

**修复一个刷新 bug**：里程碑条刚移到头部时，进度推进后头部没刷新。根因是 `State.syncMilestones` 只在阶段跳跃时回调，普通进度变化不 `notify()`。修复：进度变化即 `G.notify()`，头部监听器正常刷新。

**验收**（`_testms.tpl` + `_runms.py`，p16 + 3:4 两比例）：
- `T0 ms=0 prog=0 rows=2 next=M1/10` → `T1 rows=income,hover`（开局 2 条 ✓）
- `p40` → `T3 ms=3 prog=40 rows=5 next=M4/59` → `T4 rows=income,purchase,speed,multiplier,hover`（顺序收入上、悬停下 ✓）
- `p100` → `T5 ms=5 prog=100 rows=7` → `T6 rows=...mastery,limit,hover`
- `p50000` → `T7 ms=18 prog=50000 rows=12`（全部解锁 ✓）
- `TB toast=里程碑 M1 达成 · 基础收益统计开启 · 解锁词条 瓶身购买成本`（Toast 提示 ✓）

**改动文件**：`GameConfig.ts` / `State.ts` / `Save.ts`（无改动，只读确认）/ `GameRoot.ts` / `Drawer.ts` / `DrawerContent.ts` / `StatsPanel.ts` / `Locale.ts`。


---

## 第八轮：整屏 UI 重做为木纹卡通（用户给新主界面参考图，github 已有版本）

**用户拍板的三条**：① 全按图重构主界面；② 瓶阵形状不动（只换瓶子贴图）；③ 底部那个奶油面板（参考图里的「勺子 ×4」列表）**先不做**。

### 一、美术（AI 生成 + 程序化，共 ~16 张）
生成图（ImageGen，`E:\LDC_Fby\_art`，`_art_pipe.py` 落库）：木桌底纹、木牌、7 阶瓶子拼图、瓶盖、
金币、机器盒、滚筒、猫爪挂牌、绿叶、商城图标、奖牌图标、箭头/▲。
程序化：瓶子落地阴影 `env/disc`、光标圈 `env/areacircle`、木牌三件套 `wood_banner_l/m/r`、木轨 `wood_rail`。
落库到 `assets/resources/Textures/{env,ui,bottle}`，新贴图已全部登记进 `Core/Res.ts`。
7 阶瓶子按参考图配色重做：透明 / 绿 / 蓝 / 琥珀 / 金 / 红 / **彩虹锥瓶**。

### 二、代码（新增 / 重写）
- **新增 `UI/Theme.ts`**：`WOOD` 调色板 + `woodPlate / woodButton / chipPlate / woodBanner / woodRail /
  beltFace / woodPlateRefill / bump`，全部 Graphics 矢量绘制。
- **重写 `UI/Hud.ts`**：通栏木牌（三件拼，`fitWidth(vwE)` 随屏幕拉伸中段）+ 左返回按钮（→设置）+
  金币/瓶盖两枚深棕筹码 + 右端 3 个工具按钮（成就/统计/语言）+ 木牌下沿的 /s 速率与在途瓶盖 +
  体力细带 + 狂暴/决意状态行。
- **重写 `UI/NavBar.ts`**：金色「商城」/ 奶油「技能」/ 两枚药丸（= Drawer 两个页签，选中变金）/
  金色 ▲（收起展开「快捷购买 + 能力条」）。新 Locale key：`nav_shop/nav_skill/nav_bottle/nav_helper/nav_expand`。
- **重写 `Game/CapMachine.ts`**：**横置履带**，挂 navRoot（UI 层）。左机器盒 → 中段履带（瓶盖左→右）→
  右滚筒（回收口），闸门改**竖**闸条。世界坐标→UI 局部靠 `GameConfig.WORLD_XFORM`。
- **`GameRoot.ts`**：背景退化成「深棕兜底 + 整张木桌纹理」（删掉桌面板/桌腿）；新增 `buildDecor()`
  （猫爪挂牌 + 绿叶挂 hudRoot）；`applySafeLayout` 写 `WORLD_XFORM`、调 `hud.fitWidth`、
  收起时把舞台下沿吃到履带上沿。
- **`GameConfig.ts`**：LAYOUT / SAFE_BLOCKS / WORLD_ENV / PLAY_AREA 全部按新构图重算；
  MACHINE 增加横置履带几何；新增 `WORLD_XFORM + worldToScreen/screenToWorld`。
- **`UI/QuickBuy.ts`**：换奶油木牌皮肤；内容建在子节点 `qbRoot`（避免隐藏时连带关掉整个底部块）。
- **面板全局换肤**：`_skin_panels.py` 把冷色深蓝机械映射成「奶油木框 + 深木牌」，
  覆盖 `Drawer / DrawerContent / SkillPanel / AchPanel / StatsPanel / SettingsPanel / Panel / Toast`
  （115 处颜色）。技能树五态 SKIN 改成暖木牌配色（白线稿图标必须落在深底上才看得见）。

### 三、本轮修掉的真 bug
1. **`buildBackground()` 在资源加载之前调用** → 背景层所有 Sprite 拿到 null frame → 整块空白
   （一直以为是"深蓝背景"，其实是相机清屏色）。已移到 `Res.I.loadAll` 回调里。
2. `CapMachine` 的瓶盖染色用 `sp.color.set(...)`（就地改内部 `_color`）→ 静默失效，瓶盖永远是白的。改成整体赋值。
3. `QuickBuy` 直接隐藏 `this.node`（= navRoot）→ 收起时整块底部 UI 消失。改成自带子容器。
4. 金币筹码数字溢出压到瓶盖筹码/工具按钮上 → 数字框改到「图标右侧到筹码右沿」+ `overflow: shrink`。
5. 小字号文字描边 4px 把小字吞成"深色糊块" → 小字描边降到 2px，并把副行文字改成深棕字+奶油描边。

### 四、验收
`_testnew.tpl` + `_runnew.py`（四比例），日志与截图见 `E:\LDC_Fby\_shots\nw*.png`。
- p16：`vis=720x1281 root=1.000 worldLayer@(0,63)x1.04 hudRoot@(0,-36) navRoot@(0,-22)`，`ALL TEX OK`；
  扣盖翻转后 `PEND=33` → 履带运到滚筒后 `PEND=0`、`caps` 增加 ✓（横置履带全链路通）。
- tall（720×1609）：`hudDY=127 navDY=-185 ws=1.09`；wide（720×440, s=0.344）：整板缩小居中 ✓。
- 收起 ▲ 后 `worldLayer x1.04 → 1.09` ✓（让出来的高度给了瓶子）。

### 五、遗留 / 待办
- 技能树节点图标仍是旧的扁平 `bottle/icon_*` / `stat/*` 线稿（这些贴图本身都在 `Res.ts` 里已登记），
  与新瓶身风格不统一。建议下一轮把节点图标换成新版瓶身/图形。
- 面板换肤是机械映射，细节（里程碑条、部分次级文字对比度）可以再手调一轮。
- 底部奶油面板（参考图的「勺子」升级列表）按用户要求本轮不做。
- `1级/2级` 两个药丸按本作实际功能做成了「瓶子/助手」页签（参考图是等级页签，本作没有对应概念）。

---

## 第八轮：用户反馈三连修（设置没换肤 / 商城没换肤 / 瓶子升级重叠 + 删两张快捷卡）

用户反馈（附截图）：`设置界面没换，商城界面没换，且瓶子升级界面有重叠`，并指明截图里那
**两张快捷购买卡（普通塑料瓶 / 助手之手）不需要**。

### 一、删掉 QuickBuy（那两张卡）
- 删除 `assets/Scripts/UI/QuickBuy.ts` + `.meta`，及 `GameRoot` 里的 import / 字段 / build 调用 /
  `setRowsCollapsed` 里的显隐。底栏 ▲ 现在只收起能力条。
- ★ 连带修正：`SAFE_BLOCKS.botTopY` 原本是 **-73**（那是「快捷购买卡顶」），卡片删掉后底部块
  最上面变成能力条（`abilityY -208 + 110/2 = -153`）→ 改成 **-145**。
  不改的话展开态白白少 72px 可用高度（舞台缩放会偏小）。
- `LAYOUT.quickBuyY` 一并删除（已无引用）。

### 二、设置面板「没换肤」的真因 = 奶油底上还在用深色底板的浅色文字
- `SettingsPanel.section()` 的分区标题是 `#D8CDB8`（浅灰米色）→ 落在 `#F6E3C5` 奶油底上
  对比度约 1.3:1，等于隐形；`StatsPanel` 的「全部时间 / 瓶子」、`AchPanel` 的进度行同理。
  **一律改深棕 `#7A4210`**，并加一枚 12×30 的小木牌当行首标记。
- `rowCard()` 的默认色还停在深蓝主题（tint `#202A3C` / 描边 `#33415C`）→ 改 `#4A3420` / `#33240F`。
- 滑杆填充 `#7FE1FF`（青）→ `#E8B23C`（暖金）；开关轨道由单色改成 开=金 / 关=深木凹槽。
- 设置面板的「删除存档」行 `#2A1F22` → `#5A2A22`。
- 面板标题原来用 `ui/panel_deco` 贴图当背板 + 浅金文字，贴图自带的内框线正好压在文字上下沿，
  像「文字被切开」→ 改成 **深木铭牌 `roundedPanel` + 奶油金字**，与 Drawer 头部同一套。

### 三、★ 面板内容「上面一大片空白、全挤在下面」= ScrollView content 的 y 写错了
- `Panel.openPanel` 里 `sv.content` 的 y 原本没设（=0），而 content 锚点是 (0.5,1)
  → **内容整体下沉半个视口**：设置面板 860 高就空出上 430px。
  正确写法 `sv.content.setPosition(0, bodyH / 2 - TOP_PAD)`。
  连带把滚动区上沿从写死的「视口中心 -34」改成常量 `SCROLL_TOP = PANEL_H/2 - 120`（=420，
  正好在标题铭牌下沿 443 之下），bodyH 小的统计面板（700）不再上留 127px 空白。
- `TOP_PAD = 30`：不加的话 content 里第一行（成就那种 104 高的行、统计的分区标题）
  上半截会被视口上沿切掉，读起来像「第一行只有下半截」。
- 成就的进度行本来单独占一行，正好压住首行卡片（铭牌底 443 / 视口顶 420 之间只剩 23px，
  塞不下 26px 的行）→ 改成把进度写进铭牌（`Panel` 把标题节点命名 `ptitle`，`AchPanel` 改成
  「成就 6/24」）。`AchPanel` 首行起点从 -12 改 -40（给 104 高的行留出整高）。

### 四、★ 瓶子升级界面「重叠」= 里程碑条盖在头部条上
- `MS_Y = HEAD_Y - HEAD_H/2 + MS_H/2` 让里程碑条正好压在头部条下半截，而它是在头部**之后**
  创建的 → 渲染在上层，把金币胶囊下沿、两个页签的文字全压住了。
- 改成硬性串联：`HEAD_BOT → GAP(8) → msStrip(48) → GAP(8) → 滚动区`，
  得 `MS_Y=274 / SCROLL_TOP=242 / SCROLL_BOT=-382 / SCROLL_H=624 / SCROLL_Y=-70`
  （实测 `head 310..406 / msStrip 250..298 / scroll -382..242`，零重叠）。
- 里程碑条改成**两个页签都常驻**（助手页原来藏掉，会留一段说不清的空白）；
  进度条 240 宽 @x44、数值右对齐 @310 → 间隔 26px（原来满进度时填充条顶到数字上）。
- 品阶选择条 y 从 -46 改 -56（内容顶留 12px，避免最上一行方块描边被切）。

### 五、★ 面板开着时底栏「看得见点不动」
- Drawer 只占屏幕中间 820px，底栏本就露在遮罩外，但遮罩是全屏尺寸 + `BlockInputEvents`
  → 点「技能」只是被遮罩当成「点面板外部」关掉面板，必须点第二次才生效。
- 给 `Drawer` 加 `setMaskCut(y)`：`GameRoot.applySafeLayout` 每次重排时写入
  `LAYOUT.navY + LAYOUT.navH/2 + navDY - 6`（底栏顶沿 -6），遮罩按此**改尺寸 + 改位置**
  （锚点在中心，`top - h/2`）。底栏恢复可点，且视觉上底栏保持亮、其余压暗。
- 其余全屏面板（设置/成就/统计/技能树，高 1080）本身就盖住底栏，仍用整屏遮罩。

### 六、验收
- `E://LDC_Fby//_tfix.tpl` + `_runfix.py`（四比例）+ `_tbtn.tpl`（★ 真实点击验证）。
- `GameRoot` 把 `__tb` 扩成 `{panels, ui:{settings,ach,stats,skill}}`，
  无头验收可以直接开面板截图，不必靠盲点坐标。
- 实测：p16/tall/tab/wide 四比例 `errors: 0`；点击「商城」→ 升级面板、
  面板开着点「技能」→ 直接进技能树（不再需要点两次）、点「瓶子」药丸 → 切回升级面板 ✓。
- 源码 typecheck 0 错；构建 25~28s（exit 36 仍是假警报）。

### 七、收尾：顶栏右侧工具按钮贴边
- `LAYOUT.toolX` 228 → **214**。设计区右边界 +360，最右一枚按钮（语言）右沿原来算到 **358**，
  只剩 2px 余量，截图放大后看着像被切掉一半（用户会当成渲染 bug）。
  现在右沿 344，留 16px；左沿与瓶盖筹码之间仍有 7px 间隙。
- 顺带确认：p16 与 tall 两比例下 `bar@(0,520) 772×134`、`tool_lang@332(target 318)`、
  `contentRoot s=1` **完全一致** —— 顶栏宽度与按钮位置不随屏幕比例变化，
  差异只在 `hudDY`（p16 −36.3 / tall +127.4）。以后怀疑"某比例下 UI 错位"，
  先用 `_tlay.tpl` dump 一遍这几个值再改代码。

---

## 第九轮续：引导性 + 资源循环卡住（下午）

用户反馈三件事：① 框出的 **2 个文本**不要；② 未解锁的选项点击提示却是「金币不足」，
引导性不足；③ 没法顺利解锁下一个选项，**资源循环貌似有卡盾**。

### ① 复用的是上一轮那张截图
红框里圈的 2 个文本 = 底栏「瓶子 / 助手」两枚药丸上的字（连 ▲ 一起）。
这三枚控件在上一轮已经删掉，`NavBar.ts` 现在只有**等宽平铺**的「商城 / 技能树」。
这份截图是改动前的构建产物，不是新问题。

### ③ 卡在哪：用实测量化，不靠猜
`_tloop.tpl`（无头跑真实构建产物）直接调 `G.rollOutcome` + `G.doFlipResult` 二万次：

| 指标 | 实测 |
|---|---|
| 扣盖 / 正立 / 翻倒 | **45.2% / 41.8% / 13.0%** |
| 期望金币 | **$2.68 / 次点击** |
| 瓶盖流速（T1 = 1 枚） | 0.45 枚 / 次点击 |
| 攒 $1,000 买履带 | 364 次点击（≈4 分钟）→ **不卡** |
| 研发 T2（1,600 瓶盖） | **≈3,500 次点击（≈36 分钟）** |

而 GDD `MILESTONES` 的 M10「研发解锁 T2 铜质能量瓶」阈值是 **948 进度**，按金币流水折算
**≈350 次点击**就播报 → **「游戏说该解锁了」与「真的买得起」差了 10 倍**，
这就是「卡盾」的根因。另外实测确认履带入账链路**本身正常**（投 40 枚 → 5 秒后
`caps +40`、`pending` 归位），**不是卡死，是太慢**。

**修复**：`CAP_GAIN_BASE` `[1,2,3,4,5,6,7]` → **`[4,6,8,11,14,18,22]`**（低阶慷慨、仍逐阶递增）
→ T1 流速 1.8 枚/点击 → 研发 T2 ≈ 890 次点击（≈9 分钟）、T3 ≈ 1,780 次（≈18 分钟）；
之后靠**助手之手**（`h_unlock` 1,200 瓶盖；10 只可见手 = 6.7 次/秒，超出的走 `directFlip`）
把吞吐从 1.6 次/秒拉到 6~60 次/秒，循环自己就转起来了。
原版口径（七阶**全 1**）与折中口径（`[1..7]`）都写进 `GameConfig.CAP_GAIN_BASE` 的注释，改一行可回退。

### ② 引导性：新增 `assets/Scripts/UI/Guidance.ts`
把「点不动」拆成**带差额 + 带去处**，不再一律「资源不足」：
- `moneyShortToast` / `capsShortToast` → 「金币不足，还差 $132」/「瓶盖不足，还差 1,582 枚」；
- `tierResearchToast` / `helperResearchToast` → 「先去『技能树 · 瓶子科技』研发『铜质能量瓶』
  （1,600 瓶盖）」**并直接跳过去**（`closeShop()` + `openSkill(parent, page)`）；
- T2~T7 的购买卡：价格位不再显示「—」，改为显示**研发价（瓶盖）**；按钮「解锁」→ **「去研发」**；
  `locked_tier` 从错的「需先解锁前一阶」改成「需在『技能树 · 瓶子科技』研发」；
- 助手卡按钮同理（`go_research` → 跳助手科技页）；
- 技能树的 `need_parent` → 点名版 `need_parent_n`（「需先解锁『白银汽水瓶』」）；
- 商城面板加常驻 **「下一步：…」引导条**（`nextGoalText()`：买机器 → 研发下一阶 → 解锁助手 → 雇手）。
- `openSkill(parent, startPage)` 加了起始页签参数。
- ⚠️ 跳转必须用**注入的 NavBridge**（GameRoot `registerNav`），不能直接 import ——
  SkillPanel 反向依赖 DrawerContent（`skillDesc`），互相 import 会成环。

### 顺手修掉两个真 bug
1. **全局 `input` 不受 UI 遮罩约束**：`BottleField` 的点击/光标挂在 `input` 上，遮罩的
   `BlockInputEvents` 只拦节点事件 → **在商城面板上点任何位置都会翻一只背后的瓶子**，
   离线弹窗同样漏。修法：`Drawer.open/close` 与离线弹窗登记 `Modal.push/pop`。
2. **`Modal.count` 是模块级单例，重载场景不清**：`director.loadScene('Main')` 时若正开着面板，
   count 残留 >0 → `aim()/pointerDown()` 永远 return → **整局点不动**。
   `GameRoot.onLoad()` 增加 `Modal.reset()`。

### 验收
- 源码 typecheck **0 错**；构建 32s（`build Task (web-mobile) Finished`）。
- `_tguide.tpl`（新增）断言：初始「下一步」文案、商城开着连点 4 次不翻瓶（对比商城未开时点 1 次必翻）、
  点 T2 购买按钮后 `panel_tree_page_bottle` 出现（即跳转成功）、`TREES=` dump。
- 复跑 `_runfix.py`（四比例）确认重排没被这轮改动破坏。


## 15:28 技能树概念图（原版 Demo 数据挖掘）
- 需求：拿 BottleFlipInc Demo 技能树概念图，带解锁条件+属性标注。GDD §5 只有文字规格，无图。
- 产出：`E://LDC_Fby//BottleFlipInc//Bottle Flip Inc Demo\skilltree_concept\技能树概念图.html`（index.html 为副本，icons/ 为节点图标，来自 02_精灵切片_Sprites 的 *-04.png）。
- 数据源与方法（可复用）：
  1. 结构：UnityPy 读 `level1`，GameObject 名 `SkillTreeButtonUI_*` / `AbilitySkillTreeButtonUI_*` + RectTransform anchoredPos = 节点网格坐标；分组键走父链（Bottle/<阶位>/Content、PnH/<组>、Ability/<组>）。
  2. 边：场景 MB 无 typetree（read 报 "Expected N bytes but only 32"），但原始字节完整 → 手撸 struct 解析：SkillNodeUI 布局 = 32字节头 + Title/Desc/Desc2 三字符串 + int×4 + double + prereq List<PPtr>；`prerequisites` 存的是 **MonoBehaviour pid 不是 GO pid**，要建 mb2go 反查（漏了这步边全丢）。
  3. SkillNodeAbilityUI（Ability 页）布局更短：头 + type int + prereq list；Data 是 auto-property 不序列化。
  4. 场景里 SkillNodeUI.Data 全空、数值是 IL2CPP 常量运行时注入（resources/sharedassets/catalog 都没有 SO）→ 数值层用 GDD §5 规格标注。
  5. 每个技能面板在场景存 2~3 份布局变体（迭代残留），按 (type,x,y) 去重取最全一份。
- 原版真实结构：274 节点 / 225 前置边：7 阶位×双树（A=16 含技能槽+MachineSpeed，B=12 精简）+ PnH(Helper6/IdleMode8/MachineGate4/Area2) + Ability(飞天可乐/冲击波/狂暴/武士处决 + 被动列 + Locked×5)。
- 脚本都在 `E://LDC_Fby//BottleFlipInc//_analysis//dump_skilltree*.py` + `gen_skilltree_html.py`。

## 第十轮：商城/技能树改常驻内嵌面板 + 履带重画 + 瓶子缩小（2026-09-23 下午）
- 需求（用户参考图）：不做二级弹窗；底栏「商城/技能树」+ 右侧瓶子种类下拉框；列表统一显示在效果图下方；履带上移重画（原来变形）；瓶子缩小到 0.6。
- 实现：新增 `UI/BottomPanel.ts`（页签+下拉+常驻列表，挂 navRoot）与 `UI/UpgradeRows.ts`（紧凑行 648×78，数据驱动 RowSpec）；删除 Drawer/SkillPanel/DrawerContent/NavBar（含 .meta）。Guidance 桥改指 `showTreeCategory(page)`。
- 布局：底部块重排 abilityY −136 / beltY −256 h124 / navY −356 h72 / panelY −508 h216；botTopY −85 / botBottomY −616 / safeBottom 24；`bottleH 160→96`。
- 履带：全部改 Graphics 矢量（木框机器盒+绿面板+cap 图标+深色辊面+右滚筒），MACHINE 几何改 box150/belt340/drum150 总宽 654；「变形」根因是贴图硬拉 + 下述渲染组件 bug。
- **重大引擎坑**：一个节点只能挂一个渲染组件（warnID 12002，静默失败）——Theme 里 woodPlate/chipPlate/beltFace/woodRail 的高光/描边从项目开始就没画出来过；已全部改为子节点分层。修完后全场景 0 警告。
- 技能树选项与原版解析（`E:/LDC_Fby/BottleFlipInc/_analysis/skilltree_full.json`，274 节点/225 边，data 全空=数值运行时注入）做**结构对齐**：科技线拆成 履带升级(MachineGate+stability) / 挂机模式(光标+idle) / 手部操作(Helper) / 特殊技能(Ability)；数值层仍走 GDD §4.2 矩阵。跨栏前置不算锁（skillRows 只看栏内 parent）。
- 无头验收：购买（瓶子/机器）、下拉切换、树页词条/科技线、四比例截图全过；`_cdp.py` 加了第 8 参独立 Chrome profile（复用 profile 会让 --window-size 失效），并确认旧 chrome 残留占 9333 会让截图超时。


## 商店/升级/技能树联动页（BottleFlipInc 逆向，续）
- 用户问「为啥游戏上只有一棵技能树，你这技能树有点多」→ **用户是对的，之前把文档分组当成了游戏结构**。
- 实测铁证：场景里 `SkillTreeUI` **1 个实例**；`hudMenuButtonList` **2 条**；`SkillTreePageType` 只有 SkillTree=0/Ability=1；`Panel/Tabs` 子节点正好 Skill Tree + Ability（**试玩版整块 INACTIVE**，所以看不到切页按钮）。
- 真实结构 = **1 棵树 → 2 页签 → 一张画布（SkillTrees/Viewport/Content）9 个节点簇**：瓶阶 7 簇 + PlayerAndHelper 1 簇（Player/Helper 两个组件共用同一节点）+ Ability 1 簇。
- 四个 `*SkillTreeUpgradeUI` 类是**同一张画布上不同种类节点簇的渲染器**，不是四棵树。实例数/激活数：Bottle 16/7、Player 2/1、Helper 2/1、Ability 15/3、SkillTreeUI 1/1。多余实例 = Content vs Content(1)（Demo/FullRelease）+ Ability/Panel 下 4 个 Layout 变体。
- 新工具：`_analysis/hier_skilltree.py`（组件→GO→祖先路径）、`_analysis/verify_one_tree.py`（实例/激活统计 + SkillTreeUI 字段探测）、`_analysis/hier2.py`（层级树打印，含 INACTIVE 标记）。
- **坑：MonoScript 不在 level1 里**（MonoScript=0、MonoBehaviour=7235），解析脚本类名要用 `o.read(check_read=False).m_Script.read().m_ClassName` 跨文件 PPtr 解析，不能在 level1 里找 MonoScript 对象。
- **坑：GameObject.m_IsActive 只表示自身**，判断真正激活要沿 Transform 父链全查（`eff_active`）。
- **坑：dump.cs 枚举要程序化读**。`export_model.py` 手抄 `AbilityUpgradeType` 漏了 Gain/Cooldown/CokeCount（只有 6 个，实际 9 个）→ 技能簇表 IndexError。已加 `enum_from_dump()` 从 dump.cs 正则解析，并打印 enum 自检（UT=29 AUT=9 AT=5 BT=7）。
- **坑：model.json 的 skillTree 各组分层数不一致**：bottle 是 `{best, variantCounts}`，player/helper 是 `[变体1, 变体2]`（要多剥一层 `[player]/[helper]`），ability 直接就是列表。取法写错会拿到底层 list 而 TypeError。
- 产出：`Bottle Flip Inc Demo/shop_concept/index.html`（= `skilltree_concept/商店与升级系统.html`，7 页签）。**首页签默认不显示的坑：JS 只给按钮加 on，pane 没加** → 末尾补 `show('overview')`。

## 第十一轮：底栏改三页签 + 两列网格 + 只列可解锁项（2026-09-23 傍晚）
- 用户反馈四点：①一行 2 个选项；②下拉菜单要在下拉框正上方；③底栏排布 =【商店】【升级】【技能树】【下拉框】；④技能树列表只显示能解锁的选项。
- 页签拆分：新增「升级」页签装瓶子阶词条（tierRows），「技能树」只剩科技线（belt/idle/hand/abil）；下拉框选项跟页签走。showTreeCategory(0) 现在跳「升级」页签。
- 网格：UpgradeRows 改 makeCell（CELL 322×78 / GAP 14,10 / COLS=2），BottomPanel.rebuild 按两列排；格子内 icon40 / 名18 / 说明12 / 按钮96×48。
- 菜单定位：右缘对齐下拉框右缘（rightEdge = min(ddX+ddW/2, 360-8)），底缘贴底栏顶沿 —— 不再整屏居中。
- 过滤口径（对齐原版 model.json 梳理）：skillRows 跳过 locked+maxed；tierRows 未研发只显示研发行、已满级词条不画；空列表给「本类目已全部解锁」占位行（新 Locale 键 all_done）。
- 无头验收：底栏 x = -282/-132/28/235；菜单 x=212（右对齐 352）；升级页两格并排；履带栏买 stability(1000) 后 machineinc 自动出现（1→2 格）；0 异常。

## 第十二轮：vivo 打包环境修复（2026-09-23 傍晚）
- 现象：编辑器打包 vivo 报 `make task failed! Error: Command failed: npm.cmd install` + `'npm.cmd' 不是内部或外部命令`。
- 根因：**系统重装后没有 Node.js/npm**。Cocos 平台打包插件（`<Creator>/resources/tools/<平台>-pack-tools`）要跑 `npm install` 装依赖（vivo = `@vivo-minigame/cli-service`，提供 `mg-service`），npm 不在 PATH 就必挂在 make 阶段。
- 插件逻辑（代码在 `app.asar` 内，路径 `modules/platform-extensions/platforms/vivo-mini-game/dist/builder/hooks.js`，asar 头 16 字节后可 dump）：
  - `initPackTools()`：`node_modules` 存在且无 `.staging` → "Use the cache"；否则要求 `mg -v` 可跑，失败抛 `Please install tools: npm install -g @vivo-minigame/cli`。
  - `buildRpk()`：只要 `node_modules/.staging` 不存在 → **每次构建都调 `npm.cmd install`**（npm 必须长期在 PATH），随后 `quickSpawn("npm", ["run","build"|"release"])`。
  - `install()` **失败会 `removeSync(node_modules)`** → 失败一次就把装好的依赖删掉，下次退化成"缺 mg"错误，两种报错会交替出现（本次实测就交替了）。
- 修复（已完成）：
  1. 官方 Node LTS 放到 `D:\nodejs`。MSI 静默安装要管理员提权、当前会话拿不到 UAC → 改用官方 **zip 便携版 v24.21.0**，解压即用、完全等价。
  2. `D:\nodejs` 写入 HKCU 用户 PATH（Python `winreg` + `SendMessageTimeoutW` 广播；本机 `reg.exe` 与 PowerShell 沙箱都被限制）。
  3. `npm install -g @vivo-minigame/cli` → `mg`/`mg-service` 1.27.45（便携版默认 prefix = `D:\nodejs`，命令落在 `D:\nodejs\mg.cmd`）。
  4. `C:\Users\A\.npmrc` → `registry=https://registry.npmmirror.com`（依赖 20 个包 0.8s 装完）。
  5. `vivo-pack-tools` 目录预装 node_modules（含 `node_modules/.bin/mg-service`）。
- **坑 A**：WorkBuddy 给 shell 注入了 `NODE_OPTIONS=--require=.../node-language-shim.cjs`，npm 子进程会连带加载 safe-delete shim，操作该目录时行为异常（曾把 node_modules 弄没）。**所有 npm 操作前先 `NODE_OPTIONS= NODE_PATH=` 清空**。
- **坑 B**：编辑器进程的 PATH 是**启动时继承**的，改注册表后必须**完全重启编辑器主进程**才生效。而用项目脚本 `restart_cocos.py --full` 从 shell 重启**不可靠** —— 继承的是工具环境的 POSIX PATH，`npm.cmd` 照样找不到（实测 16:59 又挂一次）。**正确做法：用户自己重启电脑、从桌面启动编辑器**。
- 验证：模拟"重启后干净环境"（PATH = HKLM 系统 PATH + HKCU 用户 PATH，剥掉全部 `NODE_*`）跑 `node -v` / `npm.cmd -v` / `mg -v` / `mg-service -v` → v24.21.0 / 11.19.0 / 1.27.45 / 1.27.45 全通。

## 第十二轮：三页签内容按原版解析重排（2026-09-23 晚）
- 用户纠正映射：商店/升级不该用下拉框（全平铺），下拉框只属于技能树；且我此前把「瓶子词条」当升级、把「设施」当商店是错的。
- 依据原版解析 `E:/LDC_Fby/BottleFlipInc/_analysis/model.json`（shop/upgradePage/skillTree 三段）+ `report_cn.txt` §商店/升级页：
  - ShopMenuUI = 7 个买瓶按钮(随解锁出现) + 解锁瓶盖机器($1000) + 助手之手($1200起可重复买)。
  - 升级页 = 瓶子区14条(每阶 Income + FlipMode悬停翻转一次性) + 玩家区2条(光标区域大小·需光标天赋 / 瓶盖机收入·需机器) + 助手区6条(Bronze..Diamond Hand 翻瓶许可)。
  - ⚠️ 用户以为悬停翻转在商店，原版数据其实在升级页 —— 按数据放升级页，已在回复中说明可再调。
- 实现：specs() 三路 shopRows=[买瓶×N, 机器, 助手] / upgradeRows=[研发下一阶, 每阶词条(名字加「瓶子·」前缀), p_cursorsize(需p_cursor), p_machineinc(需machine), h_bronze..h_diamond(需hasHelper+对应阶)] / 树=科技线。
- 树节点重归属：p_cursorsize、p_machineinc 移出科技线（TREE_BELT_NODES/TREE_IDLE_NODES 更新）；h_*许可移出树（HELPER_CAP_NODES）。下拉框 ddNode.active 只在 tree 页签 true。
- 无头验证：商店3格/升级随研发·光标·机器·助手 3→5→6→7→8 格递增/树·履带2格/树·手部只显示可解锁3格，0 异常。

## 第十四轮：履带方向镜像 + 删顶栏速率文本（2026-09-23 傍晚）
- 用户口径：瓶盖应飞向**右端黑色滚轮**（入料机），通过履带**向左**运到**左端出售箱**回收；顶栏下方「/秒 $X」与「履带回收中 +N」两条文本删除。
- 实现：MACHINE 几何改名 box→bin(左出售箱 binX -252)、drum→feeder(右入料机 feederX +252)；beltEntryX/beltExitX 对调(+160/-160)；履带段 `c.x -= speed*dt`、闸门/回收判定改 `<=`（span 为负，gateAt 插值与缩放公式自动成立）；binLb 计数移到出售箱上方。
- Hud.ts 删 incLb/pendingLb 及 rateAcc/cInc/cPend 缓存（per_sec 键 StatsPanel 还在用，保留；belt_pending 键已无人用）。
- 无头验收：buyMachine→spawnChips(8) 早期截图瓶盖聚在右端入料口、1.7s 后全部左运入账 caps=8；顶栏下方无残留文本；typecheck 0 错。

## 第十五轮：五点反馈（2026-09-23 晚）
1. Toast 提示条移到屏幕正中（BAR_Y 430→0）；成就横幅 ACH_Y 400→110（中央偏上防重叠）。
2. 点瓶音效 TAP_SFX = ['pop3']（原 click/click2 随机）。
3. 瓶盖口径大改：树立(ok)/倒立(crit) 落地都产 **1 枚**对应品质瓶盖（State.doFlipResult 统一给 caps=1，不再 tierCapGain 多枚；fail 无）；瓶盖**不做爆散动画**直接 phase1 从瓶身中心飞出入料机（BottleField.spawnCaps 出料点改 p.x,p.y）；crit 的黑色圈圈 = Fx.shockwave（env/shockwave.png 本身是黑色同心环，tint 金色也发黑）→ 已删，只留金色星光。
4. 顶栏木牌横幅背景删除（bar/barL/barM/barR + fitWidth 清空为 no-op），按钮/筹码自带底板浮在木纹上。
5. 猫爪挂牌 + 绿叶装饰删除（GameRoot.buildDecor 清空）。
- 无头验收：ok caps=1 / crit caps=1 / fail caps=0；spawn6 全部入账；截图确认弹窗居中、顶栏无横幅、四角无装饰；typecheck 0 错。
- **第十五轮·图标与音效（用户口径）**：①删 `bottle/icon_*` 七张切图（bronze/silver/gold/emerald/ruby/diamond/classic 的 png+meta），技能/商店/下拉所有瓶阶图标改用对应阶 `body_{TIERS[tier].art}` **等比缩小**（瘦高比 0.4；注意 ruby 的 art=5、emerald 的 art=4，不能按 tier 直填）。`UpgradeRows` 新增 `iconW()`（body_* 按高 40 → 宽 16），`applyRow` 会同步 setContentSize；下拉行图标 34→宽 14。Res.ts TEXTURE_PATHS 同步删 7 条。②`click2` 定位为**瓶盖获得音效**：`CapMachine.spawnChips` 里 `playThrottled('click2','capget',90,0.55)`（hideCaps 分支不响）；UIKit 按钮默认音从 click2 改回 `click`。
- **技能树「新」标签**：存档新增 `seenModules: string[]`（Save.ts 默认 []+normalize 过滤），State 加 `moduleSeen/markModuleSeen`。BottomPanel：下拉框按钮右上角红色「新」徽标（`ddNew`，条件=hasMachine 且 cats() 里有没点过的模块，refreshHead 每帧刷）；下拉菜单行右侧同款小标签，**点选该模块才清除**（touch handler 里 markModuleSeen）。Locale 加 `tag_new`（新/NEW）。
- 验收：typecheck 0 错 → 构建 27s → `_t15.tpl` 四比例（独立 profile ×4）：dd 徽标、菜单行「新」、点选后消失、商店 body 图标、spawnChips 飞盖全部正确，控制台 0 报错。
- **第十六轮·设置面板重修（用户：文本被挡 + 风格不搭）**：三个实际 bug ①分区标题（显示/音频/语言/数据）下半截被下一行卡片盖住——卡片后建渲染在上层，标题字形最低点≈label 中心，卡顶必须低于标题中心 ≥30px → `section()` 返回值 y-56 改 **y-78**；②音量行「+」按钮(240±34)压住百分比 label(200±60)，「90%」只剩「90」→ 重排：名称(-280..-90)/「-」(-42)/条(5..155)/值右对齐至 212/「+」(254)；③风格：语言/加减按钮弃 `ui/card_white` 旧贴图改 woodButton（选中金牌 `WOOD.gold`/未选奶油），删除键弃 `btn2_red_inactive` 改 woodButton fill `#C05B3C`（砖红，DANGER 常量），开关钮奶油+金描边、开=金轨道，删除确认弹窗两键同改。**统计面板同款标题遮挡一并修**：首行 y -68→-92、第二段 y-=54→-80。验收 t17/t18：p16+wide，开关点击轨道变金/状态往返正确，确认弹窗正常，控制台 0 报错。无头验收找节点用递归 find(name) + `emit('touch-end')` 模拟点击。
- **第十七轮·四条反馈**：①删翻倒 MISS 飘字（BottleField.onLanded fail 分支）。②星光只在**倒立扣盖（crit）**出现且画在**瓶身正中间**（Bottle.ts 新增 `BODY_CENTER_DY=(ART_H*(1-2*ART_AY)/2)*BOTTLE_SCALE≈15.4`，星 y=homeY+15.4 实测与瓶心完全重合）；正立（ok）不再有星；showGain 里 crit 的第二颗星删除（保留全屏闪屏）。③影子 `SHADOW_DY` −7px（0.375*96-7=29）。④**买瓶飞入**：`RowSpec.onBuy(after, btn?)` 传被点按钮；商店行 `BottleField.queueFlyIn(tier,btn)` **必须在 buyBottle 之前**；sync 新建瓶时消费登记 → 藏真瓶子（node.active=false，tapAt/hover 循环加 `activeInHierarchy` 过滤）→ 临时瓶身精灵挂 **panelLayer（在底部商店面板之上，GameRoot.buildWorld 注入 `field.flyLayer`）** 从按钮世界坐标抛到落点（0.44s、自旋 180→540、44px 长到 BOTTLE_SCALE），落地 scheduleOnce(0) 后显形 + `flip(b)` **触发一次点击判定**（实测 flips+1）。★大坑：`spendMoney()` 内部会 notify 一次（State.ts:358），buyBottle 里它先于 bottles++ → 那次 sync 不建瓶，若 sync 尾部无条件清 flyReq 就永远对不上 → 改成 **2 秒超时作废**。⑤顶栏金币/瓶盖数字变动伸缩一次（Hud.pulse，0.2s 1→1.22→1），`moneyBumpT/capsBumpT` 锁定期间不重复触发，实测 fired1=0.2/锁内 0.184/到期后 refired=0.2。
- **无头验收技巧**：tpl 是**按行解析**的，eval 不能跨行；CDP 每行开销 300ms+，靠 sleep 采集中间帧不可靠 —— 改成①同一 eval 内同步断言（买完立即数 flyingNow/hiddenNow），②手动 `comp.update(dt)` 测锁逻辑，③`cc.game.frameRate=8` 降帧抓中间帧。强制三态：`G.rollOutcome=function(){return 'crit'}`。
- **第十八轮·「新」标签全页签化 + 未开放项隐藏**：①存档 `seenModules` 升级为**三页签共用的已读集**（State 改名 `itemSeen/markItemSeen`，技能树模块沿用裸 id；新增 `sh:b{阶}/sh:machine/sh:helper`、`up:s{阶}:{词条}/up:k{节点}`、`tr:b{阶}:{词条}/tr:m{模块}/tr:u{阶}/tr:k{节点}`）；新增 `seenBaseline` 字段——**首次构建面板时把当时可见的商店/升级项一次性标已见**（技能树模块不标：买机器前进不去，买下那一刻亮「新」正好是提示），否则开局三按钮全亮。②`RowSpec.id?` + `makeCell` 右上角 44×24「新」角标（136,26，不压价格文字），`applyRow` 按 `itemSeen` 刷 active；**点该行按钮即视为已读**（买不买得起都算，红点不长期挂）。③底栏三按钮（mkTab 里）各挂 40×26「新」角标，`unseenIds(tab)` 每 0.25s 算「当前可见且未读」集合刷新（树 = 未读模块 + 各模块内未读行）；`showTreeCategory` 跳转也会 mark。④**未开放研发的项不出现**：商店助手之手在 `!hasHelper` 时整行返回 null（原「去研发」灰按钮删除；顶部引导条 nextGoalText 的 goal_helper 仍在），升级页门控本就齐全。验收 t22（p16+wide、独立 profile）：初始三标签全灭、买机器→升级/技能树(含dd)亮、树内点行→该行标灭且新词条行依次亮新、研发 T2→商店亮+铜质能量瓶行[NEW]、研发 h_unlock→助手之手行出现[NEW]，控制台 0 报错。

### vivo 打包环境修复（19:00-19:15 收尾）
- 19:09 再次报「Please install tools: npm install -g @vivo-minigame/cli」＝编辑器仍是 16:54 由工具环境拉起的旧进程（系统未重启，开机时间仍 9-22 21:01），其 PATH 沙箱快照里没有 D:\nodejs → `mg -v` 失败。**改注册表不影响已运行进程，必须重启电脑**。
- 源码结论（hooks.ccc / cli.ccc）：make→initPackTools：`isInitVivo`(node_modules 在且无 .staging→用缓存)；否则 `isInstallVivoMiniGameTool()`＝`execSync('mg -v')`（编辑器自身 PATH）→失败抛「Please install tools」；mg 过了走 `mg init vivo-pack-tools --force`→内部跑 `npm.cmd install`（之前 npm 报错来源）。任何一步失败插件都会 `removeSync(node_modules)`。
- 已就位：D:\nodejs（node v24.21.0 + npm 11.19.0 + 全局 mg/mg-service 1.27.45，prefix=D:\nodejs）在 HKCU Path；~/.npmrc 指 npmmirror；vivo-pack-tools/node_modules 当前为缺（等重启后 mg init 自动重建，勿手动预装——失败会再删）。
- **已验证**：用「机器+用户注册表 PATH 展开后」模拟全新开机环境跑 `mg -v`/`node -v`/`npm -v` 全过 → 用户重启电脑后从桌面正常打开编辑器即可打包成功。
- **第十七轮（用户反馈：买瓶飞入动画）**：① 飞入精灵不再从格子图标大小(44px)长起，全程 BOTTLE_SCALE 真实瓶高；② 落地后不再 `flip(b)` 起跳重翻，改为 `Bottle.settle(outcome)` 直接摆落地姿态并结算（settle 复用私有 land()）。③ 用户问「瓶子竖着怎么还有 hit 音效」→ land() 里 ok 正立原来配的就是 'hit'，按口径**正立落地静音**（crit=win / fail=pop1 保留）；全工程已无 'hit' 引用。typecheck 0 错，构建 Finished 23s（仍 exit 36 假失败），产物 grep 到 `settle` 确认生效。
- **第十七轮追加（收益飘字）**：`showGain` 去掉「扣盖」前缀后，正立/扣盖统一为绿色钱数 `#3ED34F`（原来正立 #B7F7A6 太浅、扣盖金色，奶油底上看不清）；描边本来就是 Fx.floatText 池化 Label 自带的 `#141821` 宽 3，无需另加。构建 Finished 26s，产物 grep `#3ED34F` 确认。
- **第十七轮追加（选项点击反馈）**：Theme.ts 新增 `wobble(n, deg=6)`——angle 摆动 +6°→-6°→+3°→0（约 0.35s），先 `Tween.stopAllByTarget(n)` 防连点叠加；只接在底栏三页签（mkTab onClick）+ 下拉框（dd TOUCH_END），不动 scale（按压反馈自己管）。构建 Finished 27s，产物 grep `wobble` 确认。

## 第十九轮：广告商业化（2026-09-23）
- **UI/Ads.ts = 全游戏广告唯一入口**（单例 Ads.I）：`AdProvider` 接口可换 SDK
  （CrazyGames/抖音 tt/微信 wx），默认 MockProvider（延时 60ms 直接发奖）。
  `show(placement, onReward, onFail)` 全局防重入、onFail 统一 toast。
  弹窗层 `Ads.I.uiRoot` 由 GameRoot 注入 toastLayer。
- **存档 `data.adBuffs` = 绝对时间戳 ms**（coin/cap/halo）→ 离线也在倒计时；
  State 侧 `adBuffLeft/activateAdBuff` + `coinBuffOn/capBuffOn/haloBuffOn`。
  效果挂钩：doFlipResult 末尾 `coinBuffOn → amount*=2`；caps=1 → `capBuffOn?2:1`；
  `cursorRadius` ×2（halo）。常量 `AD_BUFF_SEC=180`、`OFFLINE_AD_MULT=3`（GameConfig）。
- **UI/AdButtons.ts**：三枚增益按钮挂 uiLayer 新 adRoot（屏幕中心坐标系，
  x=-288 / y=152/24/-104），金木牌 + 右下角 Graphics 画的「▶广告」角标
  （深巧克力牌底 WOOD.plate + 奶油描边 + 白三角，无现成播放图标贴图）。
  生效期整块压暗 + `Ns` 倒计时；halo 按钮 `G.hasCursor` 才显示。
- **金币不足 → `moneyShortAd(need, retry)`（Guidance）**：toast 不显示差额 +
  Ads.I.offerMoney 弹窗（不显示差额！用户口径），看完补足 gap 后 retry 重放购买。
  BottomPanel 四处接入：买瓶（带 flyIn 重放）/ 瓶盖机器 / 助手 / money 词条。
- **离线弹窗 ×3**：Hud.showOffline 加第 4 参 root —— **必须传 panelLayer**！
  原来挂 hudRoot 会被晚创建的 adRoot 盖住（遮罩压不暗 + 按钮穿透点击）。
  ×3 = 补 2× 差额、隐藏广告钮、确定钮居中。
- 验收模板 `E:/LDC_Fby/_ads.tpl`：无头跑通 增益×2/倒计时/offer 弹窗/离线×3 全链路。
  坑：`G.save()` 会把 data.last 覆盖成 now → 模拟离线要**先桩掉 G.save** 再 reload；
  Hud 没有 static I，无头脚本找节点要用 scene 递归 walk。
- **第十七轮追加（列表选项反馈）**：用户问列表点击有无动画 → 原来只有价格按钮按压缩小 0.94。`UpgradeRows.makeCell` 的按钮 TOUCH_END 里加 `wobble(root)`，整张卡片和底栏页签同款摇摆。构建 Finished 24s，产物 grep 注释确认。

## 第二十轮：列表广告图标 + 直接拉广告（2026-09-23）
- 列表买不起的金币项 → 价格按钮不显示 `$价格`，改亮 **ksp.png 摄像机广告图标**
  （源贴图在 `E:\LDC_Cocos_PJ\Cocos3X_2D\VerticalGame\VerticalGame_01\...\Public\UI_AD\ksp.png`，
  注意实际路径多一层 `VerticalGame/`；已拷进 `assets/resources/Textures/ui/ksp.png` + 登记 Res.ts）。
- 实现：RowSpec 加 `ad?: boolean`，UpgradeRows.makeCell 在按钮上预建 40×40 隐藏 adIcon
  （Sprite 'ui/ksp'），applyRow 按 spec.ad 切换：亮图标+清空文字，颜色 tint 成 TONE[tone].fg
  （买不起=压暗粉，跟着按钮态走）。BottomPanel 四处金币行挂 `ad`：买瓶/机器/助手/金币词条
  （瓶盖词条不挂，没有广告出路）。
- **moneyShortAd 改为直接拉广告（用户口径）**：不再弹 offerMoney 二级确认窗，点带图标的选项
  立刻 `Ads.I.show('money_gap')`，看完补足 gap → retry 给货（净效果=没花金币拿到货）。
  offerMoney 方法保留未删（暂无调用方）。
- 验收 `_adicon.tpl`：adIcons 亮 2 枚 → 点击后 bottles 1→2、money 3→0、**modal=false** ✅，
  金币充足后图标全部熄灭 ✅。

### 补：瓶盖项也走广告（同日第二十轮追加）
- 新广告位 `caps_gap` + `Guidance.capsShortAd(need, retry)`（与 moneyShortAd 同款：直接拉广告、
  补足瓶盖 gap → retry 给货，无二级弹窗）。
- BottomPanel 瓶盖行全部接入：unlockTierRow（研发下一阶）/ moduleRow（模块解锁）/
  skillNodeRow（光标圈大小/瓶盖机收入/助手许可）/ skillRows（科技线）/ statRow 瓶盖词条 ——
  `ad: !afford` + onBuy 失败改调 capsShortAd；capsShortToast 在 BottomPanel 已无调用。
- adIcon 尺寸按 spriteFrame 原始宽高比 contain 进 40×40（ksp 原图 46×36，不能拉方）。
- 验收 `_capsad.tpl`（桩 G.data.skills['p_cursor']=1 露出光标圈大小行）：
  图标亮 → 点击后 p_cursorsize 0→1 级、caps 0→0（净零消耗）、modal=false ✅。
- **第十七轮追加（悬停/Toast/买瓶/助手）**：① 悬停翻瓶触发循环原来整个被 `cursorOn=G.hasCursor` 挡在 update 里（没点光标天赋 p_cursor 就永不触发）→ 拆开：触发判定照跑（半径用 p_cursorsize base=55 默认值），光标圈/手指只是视觉、仍需 hasCursor。② Toast.show 的 Label 默认 overflow none，长文本画出面板 → `overflow:'shrink'` 自动缩字。③ **sync() 按阶对账重写**：原来按 seq（高阶在前）逐位比对，新阶插队首时把已有旧瓶**就地换皮**（无飞入动画+占旧位置）→ 改 wantVis/have 每阶数量对账，已有瓶一个不动、缺哪阶补建哪阶（新建正好接 flyReq 飞入）。④ 助手之手去掉 helperAllowed 许可门槛（用户口径：不挑阶，所有 idle 瓶都翻；handAct + directFlip 两处）。typecheck 0 错，构建 Finished 6s ×2，产物 grep wantVis 确认。
- **第十七轮追加（下一阶解锁入口）**：用户问「怎么只能解锁 2 种瓶子」→ 链路 ul_t2..ul_t7 本来就通，但 bottleTreeRows 里「解锁下一阶瓶」排在**收入门控之后**（该阶 income 0 级就提前 return），解锁 T2 后打开 T2 树只看到收入一条，T3 入口不可见。修复：把 unlockTierRow 提到收入门控**之前**（只在最高已研发阶出现、始终可见）；模块解锁两行仍在收入门控后。构建 Finished 28s，产物 grep 确认。
- **第十七轮追加（飞入落点=场地中间）**：用户口径「买瓶是飞到场地中间的动画」→ sync() 补建分支：flyReq 命中阶的落点改为 PLAY_AREA 中心 ±40px 抖动（防完全重叠），非购买路径仍 pickSpot 随机散布；queueFlyIn 无按钮锚点（广告补足购买）不再静默放弃，改从屏幕底部中央 (0,-600) 起飞。构建 Finished 28s，产物 grep 确认。
- **第十七轮追加（列表滚动位置保留）**：用户问「点选项为啥要刷新列表，位置应给玩家处理」→ 根因是买瓶改变指纹（bottles.join(',')）触发 rebuild，rebuild 末尾 `content.setPosition(0, SCROLL_H/2)` 恒回顶。修复：rebuild 记录 `lastCatKey=tab+':'+catId`，同页签同模块重建时保存 content.y 并夹回新高度合法范围 [SCROLL_H/2-(h-SCROLL_H), SCROLL_H/2] 恢复；换页签/换下拉模块才回顶。重建前 `scrollSV.stopAutoScroll()` 防惯性把 content 拽走。typecheck 0 错，构建 Finished 28s，产物 grep lastCatKey 确认。
